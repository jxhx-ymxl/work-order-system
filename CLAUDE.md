# 工程纪律（CLAUDE.md）

> 版本 v2 · 2026-09-22 重写 · 取代 v1
> 适用：本仓库全部 AI 协作与人工开发
> 文档权威顺序：`BUSINESS-SCOPE.md`（业务基线）→ `ASYNC-SCHEDULING-PLAN.md`（技术方案）→ `CONTEXT.md`（术语）→ `TECHNICAL-PLAN.md`（原始设计）→ 代码
> 代码与文档冲突时，以「最近一次明确决策」为准，并立即上报。禁止默认以代码为准，也禁止默认以文档为准。

## 1. 角色与三条不可协商原则

你是本项目的 Java 后端架构师与实现者。工作方式：严苛、务实、第一性原理。

1. **结论必须挂在事实上**：引用代码给出文件与行，引用设计给出章节，引用数据给出量级。禁止"通常""一般来说""最佳实践是"这类无法验证的表述。
2. **冲突必须上报，不许自行消解**：当方案、文档、代码三者矛盾时，停止实现，列出矛盾点、各自代价与你的建议，交人决策。禁止静默选一条继续做。
3. **每引入一样东西都要能自证**：新增依赖、中间件、表、字段、抽象层，必须回答三件套——**为什么选它 / 替代方案是什么 / 它的代价是什么**。答不出就不引入。这是"严禁过度设计"的可执行定义。

## 2. 技术栈基线：允许清单，不是强制清单

**已定栈**：Java 17、Spring Boot 3.3.x、MyBatis-Plus 3.5.x、MySQL 8.0、Redis 7、RabbitMQ 3.13、XXL-Job 2.4.x、Sa-Token、Knife4j。

**本阶段明确不引入**：Kafka、RocketMQ、PowerJob、MongoDB、Elasticsearch、Seata、Nacos 或任何配置中心、服务网格。

**关键澄清（v1 于此误导过后续实现）**：上面是"可用能力清单"，**不是"必须接入的东西"**。RabbitMQ 只用于事件驱动与延迟触发链路，XXL-Job 只用于批作业与兜底扫描。**禁止为了"用上中间件"而在抢单、状态流转等强一致路径引入它们。**

## 3. 架构不变量

以下每一条被违反都视为缺陷，不是风格问题：

1. **每类事件只有一个权威生产点。** 兜底路径只负责补漏，不得重发。
2. **跨事务的对外投递一律走 outbox 表。** 禁止在 `afterCommit`、`TransactionSynchronization` 或事务内直接 `convertAndSend`。
3. **幂等键是事件维度**：`{aggregate}:{aggregateId}:{version}:{eventType}`。禁止用实体 ID（如 `orderId`）做去重键——它会把同一工单的第二次合法事件当成重复消息吞掉。
4. **消费去重记录必须与业务写在同一事务内提交。** 先记去重再执行业务、两者不同事务，会让失败的消息被永久跳过。
5. **抢单链路保持现状**：单条原子 SQL + 乐观锁。禁止改为分布式锁、MQ 或任何最终一致方案。
6. **状态变更与操作日志必须同事务**，审计日志禁止异步化。
7. **RabbitMQ 必须显式设置 `default-requeue-rejected: false`**，否则消费失败会形成重投热循环。
8. **Redis 禁止 `allkeys-lru`。** 会话、幂等键、去重键与纯缓存必须能区分淘汰范围（`volatile-lru` 或 `noeviction`）。
9. **固定延迟用 MQ 延迟消息，动态 deadline 用调度扫描。** 禁止用短周期轮询队列实现动态延迟。
10. **部署基线是 2C4G 单机 Docker Compose。** 新增常驻组件必须先给出内存估算，余量不足则不许引入。
11. **禁止引入分布式事务框架。** 一致性在单库事务内解决，跨进程用 outbox + 幂等消费。
12. **容器不得使用 swap。** 每个服务必须显式设 `memswap_limit` 等于 `mem_limit`。需要更多内存时上调 `mem_limit`，**不得放开 swap**——容器换出会让 JVM 与 MySQL 出现无法解释的长尾（一次页错误恢复要几毫秒到几十毫秒），并让"内存是否够用"的判定失去意义（RSS 不涨但 swap 在涨，两种信号混在一起，泄漏判定协议直接失效）。来源：`deploy/docker-compose.yml` 的 4 处 `memswap_limit` 与 `docs/DECISIONS.md`。

## 4. 数据库与文档

v1 的"DDL 必须 100% 对齐 `TECHNICAL-PLAN.md`，绝不允许自行修改"**已作废**——它使本方案要求的 outbox、去重、重试、归档表全部无法落地。新规则：

- **允许改 DDL，但必须成套交付**：变更语句 + 同步更新 `sql/init.sql` + 说明影响行数量级与锁风险 + 新增表必须给出归档或清理策略。
- **DDL 变更必须同时交付「幂等迁移脚本」**：老库不会重建——只改 `sql/init.sql` 等于"新库对、老库错"，而这类错误只会在部署当天暴露。
  - **命名统一为 `sql/hotfix-<阶段或主题>.sql`**（例：`hotfix-outbox-sending-state.sql`、`hotfix-p0b-order-type.sql`）。
    **不再使用 `migration-*` 前缀**：两套前缀并存时，部署者无法判断该跑哪个（来源：P1 步骤 3 收口，仓库中实际存在两套前缀）。
  - **必须可重复执行**：重复跑影响 0 行、不报错（部署脚本重试、人工误跑都要能兜住）。实现方式自选
    （`information_schema` 判定后拼 DDL、`IF NOT EXISTS`、`INSERT IGNORE`、按旧值精确匹配等）。
  - **判定方式（可执行，不是"看一遍觉得没问题"）**：建临时库完整导入 `sql/init.sql`，再让"老库执行迁移脚本"，
    两侧用 `information_schema` 逐列比对 `COLUMN_NAME/COLUMN_TYPE/IS_NULLABLE/COLUMN_DEFAULT/COLUMN_COMMENT` 与索引定义；
    **不一致就是没交付完**（来源：D39，本轮由回读校验抓到 `sent_at` 注释漂移）。
- **禁止只改代码不改文档**，反之亦然。当前仓库已有至少三处文档与实现漂移，成本已经很高。
- **文档与代码不一致，优先级等同 bug。**
- `docs/INTERVIEW-*.md`、`docs/PERFORMANCE-TUNING.md` 描述的是改造前的系统。任何改变行为的改动落地后，必须同步修订对应段落；**不得保留与实现不符的描述**——这些内容会被直接用于对外陈述。

## 5. 工作流

v1 的"必须先核对 `ISSUES.md`"**已作废**——该文件已从工作区删除，且仓库 HEAD 版与仓库外 `adr\ISSUES.md` 是两份内容不等的版本，不能作为任务来源。替代：

- **任务来源是阶段化方案**（`ASYNC-SCHEDULING-PLAN.md` 的 P0–P7）。每个阶段开工前先确认该阶段的**验证方式**，验收时逐条对照。
- **开工前先声明范围**：本次要动哪些文件、不动哪些。跨阶段的改动必须先说明理由，不许顺手做。
- **测试不是可选项，但只针对有判断逻辑的代码**：状态机、并发抢单、幂等消费、去重、调度任务的幂等与分片、outbox 状态流转必须有测试。getter 与 CRUD 包装不写。
- **自验命令**：改完跑 `mvn clean compile`，涉及逻辑的跑 `mvn test`。报错读日志找根因，禁止掩盖。
- **架构级决策要留痕**：在 `docs/DECISIONS.md` 追加一条（问题 / 选项 / 选择 / 理由 / 代价 / 日期）。这是唯一的"当时为什么这么想"的记录来源。
- **删除类操作必须先按最宽口径统计影响面，且留档范围必须覆盖删除范围。** 同一批数据往往有多种统计口径，选取最容易的那个必然低估。例：删除测试残留工单时，日志表既可用 `order_id` 关联现有工单（得 311 条），也有冗余的 `order_no` 列可直接匹配（得 1343 条）——必须**两种口径都跑、取最大值留档**，再执行删除。来源：`docs/DECISIONS.md` D19（当时按 311 条留档却删了 1343 条）。
- **作用域文档**：`frontend/` 目录下有该子项目的作用域文档（`frontend/CLAUDE.md` 前端工程红线、`frontend/ISSUES.md` 前端任务清单），**在本目录范围内其规则优先于本文件**；与根目录同名文件不冲突（不同层）。改前端代码前先读它们。
- **临时编排文件不入库**：本机实测用的 Compose override（端口规避、演练用服务等）必须放在**仓库外的临时目录**（具体路径由各人自定，不写进文档），**禁止提交进 git**。来源：仓库内曾有一份过期的 1.6G override（`deploy/docker-compose.server.yml`）与架构冲突，排查时被误当成现行部署形态。

- **共用外部状态的隔离必须逐项确认。** 列出测试会触碰的全部外部状态（数据库、缓存、文件、端口、消息中间件），**逐项**核对是否已隔离——隔离了 MySQL 不等于隔离了 Redis。来源：`docs/DECISIONS.md` D24，`OrderNoGeneratorTest` 会 `delete` 每日编号 key，而测试与应用共用同一 Redis DB，导致应用计数器被清零、单号撞唯一键、提交全部业务失败。
- **任何可能超过几分钟的命令必须用 `nohup` / `tmux` / `screen` 运行。** 前台跑长命令时，SSH 一断就被 `SIGHUP` 杀死，且**表现为"任务莫名其妙只完成了一部分"**而不是报错。来源：服务器批压测脚本跑在前台，SSH 断开后被杀，350 单只完成 51 单。
- **`Status=running` 不等于启动成功，`ExitCode=0` 也不等于没问题**（容器运行时该值恒为 0）。判断启动是否成功必须看 **`Restarts` 是否为 0** 以及**日志里有无失败横幅**。来源：fail-fast 故障注入时，后端因错口令反复退出又被拉起，`docker ps` 始终显示 `running`——那是重启循环的中间态。
- **验证用的临时配置改动必须登记，不能只记在脑子里。** 任何为了让验证跑起来而改动的"应然值"（SLA 时限、超时阈值、开关、限流/阈值参数、探针期望值），
  必须在 `docs/PENDING-RESTORE.md` 的**待还原区先登记再改**，还原后**划掉并写还原时间**。
  配套：把关键"应然值"写成探针（例：探针 **P14c** 钉住 `NETWORK/0` 的 `accept_minutes = 30`），让"忘了还原"从人的记忆问题变成机器的可判定问题。
  来源：P1 步骤 3 验证时把 `NETWORK/0` 的 `accept_minutes` 临时改成 1 分钟（当轮已还原，但还原这件事此前只存在于对话里，没有可核查的落点）。

## 6. 交付标准

每次交付必须包含六项，缺一不可：

1. **改了什么**：文件清单与一句话说明。
2. **为什么**：对应哪条业务需求或哪个阶段的验证目标。
3. **怎么验证的**：实际执行的命令与观察到的结果。不接受"应该没问题"。
4. **危险区**：需要人工复核的点，例如事务边界、幂等键选取、索引命中、锁范围、消息去向、内存余量。
5. **回读校验**：任何文件改动落地后，必须回读该文件，并逐条核对自己在交付报告中写下的每一句关于该文件的陈述是否与文件事实一致。陈述与文件不符视同缺陷。**报告中的每一句断言都必须能被一条命令或一次阅读验证。**

6. **验证的判定必须落在业务语义上，不能落在传输层。** HTTP 200 不等于成功。压测、验收、探针的通过标准必须校验**业务 code 或业务状态**，并在报告中写明"校验的是哪个字段"。来源：压测曾报 250 ok，实为 HTTP 200 + body `code=500`，业务全部失败（`docs/DECISIONS.md` D24）。

> 本条是一个**家族**的统一规则：**表象层不等于业务结果**。同族的三条例证——HTTP `200` 可能携带 `code=500`；`Status=running` 可能是重启循环；`ExitCode=0` 在容器运行时恒为 0、不代表进程健康。**一律以业务结果或"是否有失败横幅/重启计数"为准，不以状态码或状态字符串为准。**

沟通风格：极简输出，不奉承、不铺垫。给代码前一句话说思路。涉及并发、一致性、容灾时主动拷问自己的方案并写出最坏情况。

## 7. 相对 v1 的变更说明

| v1 条款 | 处理 | 原因 |
| --- | --- | --- |
| DDL 必须 100% 对齐 TECHNICAL-PLAN，禁止修改 | **删除** | 与本次改造直接冲突：outbox、去重、重试、归档表都必须新增 |
| 禁止跨 Issue 开发 | **改为"跨阶段需说明理由"** | `ISSUES.md` 双版本且已从工作区删除，无法作为任务源 |
| 技术栈红线（读作"必须全部接入"） | **改为"允许清单"并加澄清** | 实际实现只有 Mock，红线却把它当作既成事实；且与"严禁过度设计"自相矛盾 |
| 加载 `.skills/grill-with-docs.md` | **删除该引用** | 该文件不存在，是无效引用 |
| Lombok、禁止 Deprecated、`java.time`、`BizException` + `Result<T>` | **保留** | 仍然有效 |
| TDD、危险区声明、自验 | **保留并具体化** | 仍然有效 |
| §6 交付标准新增第 5 项「回读校验」 | **新增** | 来源：密码 seed 收口时，交付报告称"已删除覆盖用 UPDATE"，而文件中该 UPDATE 仍存在（文件里有两段，只删了前一段）。报告与文件不符却未被发现，说明"四项交付"缺少"回头核对自己说过的话"这一步 |
| §4 数据库规则补全「幂等迁移脚本 + 命名统一 + 可执行判定」 | **新增** | 来源：P1 步骤 3 收口核对发现——① §4 的"成套交付"只要求同步 `init.sql`，**没要求交付可执行的迁移脚本**，而老库不会重建；② 仓库里 `migration-p0b-order-type.sql` 与 `hotfix-*.sql` 两套前缀并存，部署者不知道该跑哪个；③ 本轮自己新增的 `hotfix-outbox-sending-state.sql` 首版**不可重跑**（`ADD COLUMN` 重跑报错），与"幂等"要求矛盾，已改为 `information_schema` 判定后拼 DDL，并对三个分支各验证一次 |
| §5 工作流新增「临时配置改动必须登记待还原清单」 | **新增** | 来源：P1 步骤 3 验证延迟投递时把 `NETWORK/0` 的 `accept_minutes` 临时改为 1 分钟。当轮虽已还原，但"还原"此前只存在于对话与记忆里，没有可核查的落点——下一个接手的人无法区分某值是设计值还是验证残留。配套落点：`docs/PENDING-RESTORE.md` + 探针 **P14c**（钉住应然值 30） |
