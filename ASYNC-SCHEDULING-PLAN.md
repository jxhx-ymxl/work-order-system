# 异步与调度改造方案（work-order-system）

> **场景定位**：高校后勤 / IT 报修工单
> **目标部署**：2C4G 轻量服务器，全部组件用 Docker Compose 跑在同一台机器
> **依据材料**：`PROJECT_MAP.md`、`TECHNICAL-PLAN.md`、`RABBITMQ-MIGRATION.md`、`deploy/` 编排文件，以及现有源码（`MessagePublishService` / `MockMessagePublishServiceImpl` / `ReleaseTimeoutScheduler` / `SlaEscalationScheduler` / `WorkOrderServiceImpl` / `NotificationServiceImpl` / `OrderLogAspect` / `application.yml`）
> **本轮边界**：只出方案，不改任何代码；所有结论都标注了理由与代价

---

## 结论速览

| 议题 | 结论 | 一句话理由 | 代价 |
| --- | --- | --- | --- |
| 4G 是否够用 | **够用但紧**，余量约 **0.8–1.4G**（v1 曾写 1.4–2.0G，已按自身公式重算，见 §1.3） | 7 项常驻 RSS 合计 **2.3–2.9G**；v1 把两个 JVM 组件的 RSS 算低于自己声明的公式下限 | **2 vCPU 与磁盘 IO 才是真瓶颈**；必须放弃 admin 高可用；堆与 buffer pool 改为按需上调而非 P0 先调 |
| MQ 选型 | **RabbitMQ** | 本项目要的是低吞吐 + 可靠投递 + 灵活延迟，不是高吞吐流处理 | 吞吐上限远低于 Kafka；延迟能力依赖社区插件 |
| 调度选型 | **XXL-Job 2.4.x** | `pom.xml` 已声明该依赖，团队零学习成本，admin 单 jar 可塞进 4G | admin 是额外一个 Spring Boot 进程（约 +0.4G）且为单点 |
| 真正需要 MQ 的业务点 | 3 个：接单超时**固定延迟**触发、SLA/升级告警、提交后通知推送 | 需要"进程重启不丢 + 可重试 + 多实例安全"，线程池给不了 | 多一套中间件与一套运维负担 |
| 只需调度即可驱动 | 历史归档、周期报表、失败重试扫描、**outbox 补投的触发** | 本质是"定时批量作业 / 定时补偿"，不是事件流 | 调度精度受任务周期限制（分钟级） |
| **补投的投递必须走 MQ** | outbox 记录的最终投递 | outbox 只是**跨事务投递的载体**，它不自己送消息：投递动作必须经过 MQ 完成 | 不能把 outbox 理解成"不需要 MQ 的替代队列"——它替代的是"事务内直发"，不是 MQ 本身 |
| 明确"不需要" | 抢单、状态机流转、操作日志、实时看板、分布式事务、ES | 见 2.8 逐条理由 | — |
| 改造最大风险 | **生产端双写漏洞**：`afterCommit` 里发消息 | commit 成功、send 之前进程崩溃 → 消息永久丢失且无任何记录 | 修复要引入 outbox 表与独立投递任务 |
| 兜底路径 | **本地 `@Scheduled` 兜底默认开启，与 xxl-job 并行** | 需要人工判断的开关在故障时没人会去开（v1 的"默认关闭"已撤销） | 两套调度并行会产生重复触发，靠状态守卫吸收，代价是日志噪音 |
| 实施顺序 | **P0 → P1（含 outbox）→ P4 → P5 → P2 → P6 → P7**，合计 **18 人日** | 让第一个业务亮点（提交 P99 5s → 100ms）尽早到达；P1 与原 P3 合并避免重写 | 严格串行、总工期更长，换来每阶段都建在已验证的地基上 |

---

## 一、资源占用核算

### 1.1 前提与业务量级假设

内存账必须挂在业务量上才有意义，先声明假设（这些数字是估算，不是实测，用于定容量量级；真实值需按 P0 阶段的方法测一次）：

- 单校区 2 万师生，日均新增工单 **300–800 单**，峰值 **3–5 单/分钟**。
- 消息吞吐需求 ≤ **10 msg/s**（含通知、事件、补偿）。
- 保留期：在线数据 2 年，`t_work_order` 量级 10⁵ 行、`t_work_order_log` 10⁶ 行。

**这个量级决定了选型方向**：我们不是在为吞吐买中间件，而是在为"可靠性语义 + 延迟精度 + 多实例安全"买中间件。任何"因为它能扛百万 QPS"的选型理由在本项目都不成立。

### 1.2 逐组件估算

估算方法：JVM 类组件 `RSS ≈ 堆 + Metaspace + Code Cache + 线程栈 + JVM 固定开销(约 100–150M)`；MySQL `RSS ≈ buffer_pool + 连接级缓冲 + 数据字典 + 固定开销(约 150–250M)`。

| 组件 | 镜像 / 形态 | 常驻 RSS 估算 | 容器上限建议 | 关键参数建议 |
| --- | --- | --- | --- | --- |
| MySQL | `mysql:8.0` | **600–700M** | `mem_limit: 1g` | `innodb_buffer_pool_size=256M`、`innodb_log_buffer_size=16M`、`max_connections=100`；如需再省 100–200M 可评估 `--performance_schema=OFF`（代价：失去部分在线诊断能力） |
| Redis | `redis:7-alpine` | **70–100M** | `mem_limit: 256M` | `maxmemory 128mb`（与 `deploy/docker-compose.yml` 的实际值对齐）；**淘汰策略必须从 `allkeys-lru` 改为 `volatile-lru` 或 `noeviction`**，理由见 1.5。**为什么是 128mb 而不是 192mb**：AOF 已开启（`--appendonly yes`），重写期间会产生额外的内存与磁盘开销；在 `mem_limit: 256M` 下留 128M 余量比留 64M 更安全——本项目 Redis 里存的是会话、幂等键与去重键（总量小），128mb 足够，余量优先留给 AOF 重写 |
| RabbitMQ | `rabbitmq:3.13-management` | **180–260M** | `mem_limit: 512M` | `vm_memory_high_watermark.absolute=384MB`、`disk_free_limit.absolute=1GB`；容器内**必须用 absolute 而非比例**，理由见 1.5 |
| xxl-job-admin | 2.4.x（Spring Boot 单 jar） | **400–500M** | `mem_limit: 768M` | `-Xmx192m -Xms128m -XX:MaxMetaspaceSize=128m`；需在同实例建独立 `xxl_job` 库（额外连接 + 心跳写入，十几 MB 级）。**v1 写 320–400M 是低估，按 1.2 的公式重算见下方明细** |
| 后端应用 | 现有 `Dockerfile`（alpine JRE） | **700–900M** | `mem_limit: 1g` | `-Xmx512m -Xms256m -XX:MaxMetaspaceSize=192m -XX:MaxDirectMemorySize=64m`；**注意这是"改造后目标值"，P0 阶段不调（理由见 1.4）**。v1 写 550–700M 是把 512m 堆按 1.2 自己的公式算漏了，重算见下方明细 |
| 前端 Nginx | `nginx:1.27-alpine` | **20–35M** | `mem_limit: 64M` | `worker_processes 1`；静态资源走页缓存，不进 RSS |
| Docker / 系统底噪 | dockerd + containerd + journald + sshd | **300–400M** | — | 不由 compose 管，必须计入 |

**JVM 组件重算明细（v1 与自身公式矛盾的修正）**：v1 声明的公式是"堆 + Metaspace + Code Cache + 线程栈 + JVM 固定开销(100–150M)"，但给出的 RSS 值明显低于按公式相加的结果。逐项列出：

| 组件 | 堆 | Metaspace（实际占用） | Code Cache | 线程栈 | JVM 固定开销 | 合计 |
| --- | --- | --- | --- | --- | --- | --- |
| 后端应用（`-Xmx512m`） | 512M | 80–120M（**取实际占用，不是 `MaxMetaspaceSize=192m` 上限**） | 40–60M | 40–60M | 100–150M | **772–902M → 记 700–900M** |
| xxl-job-admin（`-Xmx192m`） | 192M | 80–120M | 40–60M | 30–50M | 100–150M | **442–572M → 记 400–500M** |
| （对照）后端 v1 取值 | — | — | — | — | — | 550–700M ← **低于公式下限**，v1 自相矛盾 |

**关于 Metaspace 的口径**：`MaxMetaspaceSize` 是上限、不是占用。Spring Boot 应用实测常驻 Metaspace 一般在 80–120M 区间；若按上限（192m）计入，会把估算虚高，若按 0 计入（v1 实际做法），会把估算虚低。本文件统一取 80–120M。

**交叉验证**：`deploy/README.md` 记录了瘦身版（MySQL buffer 128M、JVM `-Xmx256m`、无 MQ/XXL-Job）在 2C2G 上实测 1.3–1.5G。注意该实测对应的是 `-Xmx256m` 而非 512m，不能直接外推——本方案的新增项为 RabbitMQ（+0.2G）与 xxl-job-admin（+0.4G），且后端堆若上调到 512m 还要再加约 +0.25G，得到 **2.3–2.9G**，与上一行的重算结果一致。

**待实测标注（本轮新增，最可疑的一项）**：上表 **RabbitMQ 的 180–260M 是本表中最可疑的低估项**——该区间更接近"纯 broker、无插件、无堆积"的空载值，而本方案要用的是 `rabbitmq:3.13-management`（management 插件自带 HTTP 服务与统计聚合）且带 Erlang 运行时，实际常驻开销通常高于该区间。**P0a 第 3 项实测时必须单独记录 RabbitMQ 的空载 RSS**；若显著超出（例如超过 350M），**回改本表与 §1.3 的余量结论**，并重新评估"4G 够用但紧"这一结论是否仍成立。

### 1.3 合计与结论

```
MySQL 600–700 + Redis 70–100 + RabbitMQ 180–260 + xxl-job-admin 400–500
  + 后端 700–900 + Nginx 20–35 + 系统底噪 300–400
  ≈ 2.3–2.9G（常驻 RSS 估算区间）
```

**4G 够不够用：够用，但余量不大。** 按标称 4G 计算余量 **1.1–1.7G**；扣除内核与不可用内存（标称 4G 的机器实际可用常在 3.7G 上下）后为 **0.8–1.4G**，即**不到总内存的 1/3**。结论从 v1 的"内存不是瓶颈、余量 1.4–2.0G"**下调为"够用但紧，且必须实测"**。

> **2026-09-23 实测更新**：本机 6 容器预演实测合计 ≈1.2 GiB（含 rabbitmq 155 MiB、xxl-job-admin 299 MiB），余量约 **2.4–2.5 GiB**——**本节基于公式的估算系统性偏高**（RSS 只统计已触碰页，不会一开始就吃满 `-Xmx`）。详见 §1.6；本节的公式估算保留作为**上界**参考。结论仍以服务器批的 2C4G 实测为最终依据。

> 口径说明：你给的余量区间是 0.6–1.3G，与我按公式逐项相加得到的 1.1–1.7G（标称口径）/ 0.8–1.4G（实际可用口径）有差异，差异来自"4G 是否指实际可用内存"以及系统底噪取值。本文件采用**较保守的下限表述：0.8–1.4G**，并按 0.6G 作为压力下限来做风险判断。

**v1 的结论错在哪**：v1 用 550–700M 表示 512m 堆的后端，用 320–400M 表示 192m 堆的 admin，两项合计低估约 0.3–0.5G；再加上"系统底噪 350M"是乐观取值，最终把余量算成了 1.4–2.0G。**余量被高估 0.5–0.7G，恰好是"再加一个常驻组件"的量级**——这就是必须重算的原因。

余量的三个用途，缺一不可：

1. **构建期瞬时占用**：`npm run build` + `mvn package` 同时跑会瞬时吃掉 1G+。部署时必须串行构建，或用 CI 出镜像、服务器只 `pull`。
2. **RabbitMQ 消息堆积**：延迟消息与死信会占内存与磁盘，流量异常时余量就是缓冲。
3. **避免 OOM Killer 误杀 MySQL**：没有余量时内核会挑 RSS 最大的进程下手，通常就是 MySQL。

**瓶颈在哪个组件**：内存维度上，最大单项是 **MySQL（占常驻的 25%–30%）**，但真正会先出问题的是 **CPU 与磁盘 IO，不是内存**：

- **2 vCPU 被 7 个容器分摊**：MySQL、RabbitMQ、xxl-job-admin、后端四方争抢。RabbitMQ 在消息堆积/大量延迟消息到期时会明显吃 CPU；xxl-job-admin 有调度线程池与心跳；后端还要跑消费线程。这是本方案最可能先触顶的资源。
- **磁盘 IO**：MySQL redo/binlog、RabbitMQ 持久化与 mqueue、AOF 重写会在同一块云盘上争抢。低配云盘（如 20–40 IOPS 档）是真实瓶颈。
- **内存上限是"上限"不是"预留"**：上表 7 项 limits 相加约 4.3G > 4G，这是允许的（cap 不是 reservation），但必须按 RSS 估算来规划，不能靠 limits 相加判断安全性。
- **组合态才是真峰值**：单项常驻只是基线。**压测 + 归档任务 + 高连接数同时发生**时，MySQL 的连接缓冲与排序缓冲、后端的消费线程栈、RabbitMQ 的堆积都会同时涨——这个组合态的 RSS 与 CPU 必须单独测（验收方式见第六部分 P0a 第 3 项）。**单个组件不超限，不代表同时跑不超限。**

**另外三条硬建议**：

1. 开 **2G swap**，设 `vm.swappiness=10`，仅作 OOM 兜底；同时给 MySQL/RabbitMQ 显式设 `mem_limit`，避免被动换出导致延迟抖动。**但不要假定 swap 一定可用**：容器内进程能否真正使用宿主机 swap 取决于 `memswap_limit` 语义，不同 Docker / cgroup 版本（cgroup v1 与 v2）默认行为不同——`swappiness=10` 是否生效必须在 P0 实测，不得假定（验收方式见 P0a 第 4 项）。
2. 若预算允许升到 **2C8G**：MySQL buffer pool 可提到 1–2G，后端堆提到 1G，磁盘缓存更从容——这比"再加一个中间件"划算得多。
3. 若要求 **xxl-job-admin 高可用或 MySQL 主从**：4G **不够**（仅一个 MySQL 从库就 +0.5G），此时应上独立机器，不要在同一台 4G 里堆高可用。

### 1.4 参数基线（已定稿：堆 512m + buffer pool 256M；"按需上调"待办已取消）

v1 把"后端堆 256m→512m"和"MySQL buffer pool 128M→256M"列为 P0 必改项。**这条已改**，理由：这两项是为**改造后的负载**（MQ 消费线程、outbox 投递、归档与报表的范围扫描）准备的，而 P0 阶段这些负载一个都还没上线——**先吃掉 0.25–0.3G 余量，收益为零**。

| 参数 | P0 阶段 | 何时上调 | 上调信号（监控指标） | 代价 |
| --- | --- | --- | --- | --- |
| 各容器 `mem_limit` / CPU 限制 | **P0 必做** | — | — | 需要为每个服务设定真实上限并监控接近度 |
| 后端堆（256m → 512m） | **不动** | P1 之后（消费线程上线）、P4 之后（幂等与退避上线） | GC 日志：Full GC 频率上升、单次停顿变长；容器 RSS 接近 `mem_limit` | 常驻 +150–200M |
| MySQL `innodb_buffer_pool_size`（128M → 256M） | **不动** | P6 归档上线前后（范围扫描变多） | `Innodb_buffer_pool_reads` 持续增长（命中率不足）；慢查询里出现大量物理读 | 常驻 +130M |
| 开 2G swap + `swappiness=10` | **P0 必做，且必须实测有效性** | — | 见 1.3 第 1 条：容器能否用宿主机 swap 取决于 `memswap_limit`，不同 cgroup 版本行为不同 | 换出会带来延迟抖动，故只作 OOM 兜底 |

**新做法**：P0 先把上限与监控立起来、把当前 4 容器的真实 RSS 测出来（P0a 第 3 项）；堆与 buffer pool 在 P1/P4/P6 之后**按监控信号逐级上调**，每次只动一档并复测余量。**"按需"不等于"不调"，而是"不在没有负载的时候先调"。**

**定稿结论（P0a 收尾，2026-09-23，取代本表"按需上调"的三行）**：

- **后端堆 = `-Xmx512m -Xms256m -XX:MaxMetaspaceSize=192m -XX:MaxDirectMemorySize=64m`，MySQL `buffer_pool = 256M`，二者已写入 `deploy/docker-compose.yml` 作为基线**（不再是待办）。
- **取消原条件式规则**："必须先在 4G 受限环境跑出无 OOM 才落基线"——该条件建立在一个**不必要的验证**上。新依据是三件事的组合：① 参数本身有硬上界（`mem_limit` 与 `-Xmx` 都是封顶值，RSS 不可能无限增长）；② 按上界推算 4G 下余量约 1 GiB（见 §1.6 双口径）；③ 目标参数下 6 容器实测（含 30 分钟浸泡 + 突发压测）未出现 OOM 与重启。
- **保留一条复核动作**：服务器批用 `free -m` 与 `docker stats` 复核整机余量；**若实测余量显著低于 1 GiB，再回收**（下调堆或 buffer pool）。

---

### 1.5 两处会引发正确性问题（不只是性能）的配置

1. **Redis `--maxmemory-policy allkeys-lru` 会驱逐业务关键键**。当前 Redis 存的不只是缓存：Sa-Token 会话（`sa-token:...`）、抢单幂等键 `token:reject:*`、SLA 去重键 `sla_notified:*` 都在同一个实例里。`allkeys-lru` 在内存打满时会无差别淘汰，后果是**用户被静默登出、去重键失效导致重复告警**。应改为 `volatile-lru`（只淘汰有 TTL 的键）或 `noeviction`（写满报错并由监控发现）。
2. **RabbitMQ 容器内按比例设置内存水位会失真**。`vm_memory_high_watermark` 默认按 0.6 计算"可用总内存"，在容器里可能读到宿主机内存而非 cgroup 限制，导致水位永不触发、消息堆积到 OOM。必须显式设 `vm_memory_high_watermark.absolute=384MB`。

---

### 1.6 本机实测（阶段 2 预演，2026-09-23）

#### 1.6.1 修正对比表（三列口径，取代 §1.6 上方表；2026-09-23 补测）

**口径声明（不可省略）**：三列**不可混用**。用 `-Xmx256m` 的实测值去对 `-Xmx512m` 的估算值是不可比的——所以下表把"实测（注明参数）/ 上界（限额设定值）/ 原估算（v2 公式值，已被 D21 降级为上界）"分成三列。本轮实测用的是**目标上界参数**：backend `-Xmx512m -Xms256m -XX:MaxMetaspaceSize=192m -XX:MaxDirectMemorySize=64m`、mysql `--innodb-buffer-pool-size=256M`、xxl-job-admin `mem_limit=512M`。

| 容器 | 实测 RSS（目标参数；空载 → 50 单/分钟峰值） | 上界（mem_limit / 关键设定） | 原估算（v2 公式，已降级为上界） |
| --- | --- | --- | --- |
| mysql:8.0 | **387 → 386 MiB** | 1 GiB / buffer pool 256M | 600–700M |
| redis:7-alpine | **7.8 → 8.3 MiB** | 256 MiB / maxmemory 128mb | 70–100M |
| backend（`-Xmx512m`） | **288 → 295 MiB** | 1 GiB / 堆 512m | 700–900M |
| frontend（nginx） | **14.8 → 14.8 MiB** | 64 MiB | 20–35M |
| rabbitmq:3-management | **161 → 127 MiB** | 512 MiB | 180–260M |
| xxl-job-admin（`-Xmx192m`） | **349 → 337 MiB** | **512 MiB**（本轮由 768 收紧） | 400–500M |
| **6 容器合计** | **≈1.18 GiB → ≈1.14–1.23 GiB** | — | 2.3–2.9G |

**两条必须随表引用的说明**：

1. **Redis 与 MySQL 的空载 RSS 不是稳态值**：两者都是"按需增长"的存储进程（Redis 随键增长、MySQL 随 buffer pool 命中增长），空载 RSS 只反映已触碰页。**容量规划应以上界列（`mem_limit` / `maxmemory` / `buffer_pool`）为准**，实测列只用于判断"当前负载下还差多少"。
2. **系统底噪在 WSL2 上测不出可信值**：容器内看到的 `/proc/meminfo` 是 Docker Desktop 虚拟机的视图，不等于云主机。**底噪与整机余量必须以服务器上的 `free -m` 为准**，本表只覆盖容器自身。

环境：Docker Desktop 4.88.1 / WSL2 后端，宿主 20 CPU / **7.612 GiB**（**未**限制为 2C4G，见下方"口径限制"），
容器限额按 §6.3 写入。负载：空载 + 通过 API 持续 **50.5 单/分钟 × 5 分钟（250 单，0 失败）**。

| 容器 | 限额 | 空载 RSS | ≈50 单/分钟下 RSS | §1.2 估算 | 实测 vs 估算 |
| --- | --- | --- | --- | --- | --- |
| mysql:8.0（buffer pool 128M） | 1 GiB | 444.4 MiB | 453.2–454.3 MiB | 600–700M | 低 ~150–250M |
| redis:7-alpine | 256 MiB | 6.19 MiB | 6.44–7.32 MiB | 70–100M | 低 ~65–93M |
| backend（镜像默认 `-Xmx256m`） | 1 GiB | 215.3 MiB | 236.4–248.6 MiB | 700–900M（对应 `-Xmx512m`） | **口径不同，见下** |
| frontend（nginx） | 64 MiB | 16.21 MiB | 14.83 MiB | 20–35M | 低 ~5–20M |
| **rabbitmq:3-management** | 512 MiB | **155.0 MiB** | 155.6–156.0 MiB | 180–260M | **低 ~25–105M（推翻"低估"假设）** |
| **xxl-job-admin 2.4.0**（`-Xmx192m`，健康态） | 768 MiB | **299.2 MiB** | 325.4–333.9 MiB | 400–500M | 低 ~100–200M |
| **6 容器合计** | — | **≈1.09 GiB** | **≈1.16–1.18 GiB** | 2.3–2.9G | **低 ~1.2–1.7G** |
| （对照）4 容器合计（mysql+redis+backend+frontend，压测后） | — | — | **≈724 MiB** | — | 与 `deploy/README.md` 的瘦身版口径（≈1.1–1.2 GiB，含系统底噪）互相印证 |

**为什么估算会系统性偏高**：RSS 统计的是**已触碰（touched）的物理页**，而 JVM 不会在启动时 commit/touch 满 `-Xmx`。
§1.2 的公式"堆 + Metaspace + Code Cache + 线程栈 + 固定开销"把 `-Xmx` 当成了常驻量的下限，对空载与轻载场景**必然高估**。
本表把这一条从"公式推算"升级为"实测"，并保留原估算作为**上界参考**（堆被真正填满时才会接近它）。

**结论更新（§1.3 的修正）**：6 容器实测 ≈1.2 GiB + 系统底噪 0.3–0.4 GiB ≈ **1.5–1.6 GiB**，
2C4G 余量约 **2.4–2.5 GiB**（原估 0.8–1.4 GiB）。**"2C4G 够用"的结论不变，且余量比原方案宽得多**；
同理 §1.4"按需上调堆与 buffer pool"的空间也比原判断更充裕。

**口径限制（不得省略，结论以此为准）**：

1. 只有空载与 50 单/分钟（10× 峰值）两种状态，**没有长时间稳定性测试、没有堆压力测试**；
2. backend 用的是镜像默认 `-Xmx256m`，**不是 §1.4 的目标 `-Xmx512m`**——堆调大后 RSS 会随触碰页上升，不可直接外推到 512m；
3. 宿主内存为 7.612 GiB 且**未限制为 4G**，因此"4G 下会不会 OOM、余量被挤压到什么程度"**没有实测**；
4. mysql 用的是 compose 里的 `buffer_pool=128M`，不是 §1.2 建议的 256M；
5. xxl-job-admin 的实测值是**导入官方 `tables_xxl_job.sql` 后的健康态**（日志确认 `init xxl-job admin success`），不是失败态。

---

> **⚠ admin 的两组实测值不可混比（2026-09-26 追加；呼应 §1.6.1 开头的"三列不可混用"口径声明）**
>
> · **`-Xmx192m`（预演，RSS 299.2 MiB）**——§1.6.1 第 6 行与 §1.6.2 上方的明细表用的都是这个参数；
> · **`-Xmx256m`（P2 步骤 1 部署实测，RSS 358.8 MiB / `mem_limit` 上限 512 MiB）**——
>   **compose 里实际部署的是这一组**（`JAVA_TOOL_OPTIONS: -Xmx256m -XX:MaxMetaspaceSize=128m`；
>   为什么**不能**用 `JAVA_OPTS` 见 §1.6.8 教训 2）。
>
> 两者**堆上限不同**，差的 ~60 MiB 正来自堆本身（256−192=64M 量级）：**不能拿 299.2 MiB 去推部署后的占用**，
> 也不能反过来用 358.8 MiB 否定预演结论。**容量规划仍用 §1.6.2 的上界口径**（admin 按 `mem_limit` 量级计入），
> 实测值只用于回答"这台机器现在还剩多少"。**旧行保留不覆盖**——它们是当时的真实读数，改掉就丢了"参数变了"这条信息。

#### 1.6.2 余量口径（双口径并列；容量规划必须用后者）

| 口径 | 数值 | 用途 | 为什么 |
| --- | --- | --- | --- |
| **轻载实测口径** | 6 容器 **≈1.23 GiB**（47 单/分钟负载下的实测峰值） | 只说明"当前负载下实际占用多少" | 反映的是已触碰页；负载一升，RSS 会继续向 `-Xmx` 上限爬 |
| **容量规划口径（应采用）** | 按参数上界推算 **≈2.7 GiB（含系统底噪）**；4G 机器实际可用 ≈3.7 GiB 下 **余量 ≈1 GiB** | 判断"这台机器够不够" | 容量规划必须按**上界**算：堆 512m + metaspace 192m + direct 64m + 线程栈，mysql buffer pool 256M + 固定开销，各容器取 `mem_limit` 量级，再加系统底噪 0.3–0.4 GiB |

**方法学（两个方向的错误都要留痕）**：

- **方向一（已由 D21 记录）**：把 `-Xmx` 当成 RSS 的**下限**去相加 → v2 最初估算 2.3–2.9G，属于**高估**（RSS 只统计已触碰页，空载时远低于堆上限）。
- **方向二（本节记录）**：把**轻载实测值**当成容量规划依据 → 1.23 GiB 看似富余极大，但那是"负载还没把堆用满"的状态，属于**低估**未来的真实占用。

两者是同一类错误的两个方向：**都在把"某个时刻的瞬时值"当成"规划用的边界值"**。正确做法是——**规划看上界，运行看实测，两者分开写、不允许互相替代**。因此本文档同时保留：上界列（§1.6.1，用于规划）与实测列（用于判断当前状态与发现泄漏）。

---

#### 1.6.3 30 分钟浸泡 + 突发压测（泄漏观察，2026-09-23）

形态：6 容器（目标上界参数）空载静置 30 分钟（每 5 分钟采样）→ 突发 50 单/分钟 × 5 分钟（**校验业务 code**）→ 再静置 10 分钟。单位 MiB。

| 时点 | frontend | backend | xxl-job | redis | rabbitmq | mysql | **合计** |
| --- | --- | --- | --- | --- | --- | --- | --- |
| t=0（刚起） | 17.51 | 279.9 | 336.6 | 9.19 | 161.2 | 461.2 | **1265.6** |
| t=5min | 15.82 | 272.9 | 325.0 | 8.72 | 126.9 | 411.0 | **1160.3** |
| t=10min | 15.61 | 272.9 | 326.9 | 9.08 | 126.6 | 411.8 | **1162.9** |
| t=15min | 15.47 | 273.2 | 328.1 | 8.82 | 127.3 | 411.4 | **1164.3** |
| t=20min | 15.39 | 273.4 | 328.2 | 8.96 | 126.6 | 412.0 | **1164.5** |
| t=25min | 15.39 | 273.2 | 328.7 | 8.50 | 127.1 | 412.2 | **1165.1** |
| t=30min | 15.39 | 273.0 | 329.0 | 8.73 | 127.2 | 411.2 | **1164.5** |
| 突发结束 | 15.39 | 288.1 | 329.2 | 10.10 | 126.2 | 414.2 | **1183.2** |
| 静置 +5min | 15.39 | 286.7 | 334.9 | 11.00 | 126.9 | 413.4 | **1188.3** |
| 静置 +10min | 15.39 | 286.0 | 338.6 | 11.26 | 127.0 | 413.9 | **1192.2** |

**突发压测结果（校验字段：响应体 `code`，不是 HTTP 状态）**：`ok=250 / bizfail=0`，297 秒，**50.5 单/分钟**；**P50 25.1ms、P95 38.5ms、P99 167.7ms、max 331.1ms**。

**结论与判断**：

1. **空载 30 分钟无爬升趋势**：合计在 1160.3–1165.1 MiB 之间波动，**非单调**，25 分钟总漂移 <5 MiB（≈0.2 MiB/min 且未累积）。**未发现空载泄漏。**
2. **突发后 15 分钟合计 +27.7 MiB**（backend +13、xxl-job +9.6、redis +2.5、mysql +3，其余持平）。**方向向上但幅度 <3%，且没有任何容器接近其 `mem_limit`**。诚实判断：**这不足以认定为泄漏**——JVM 在压力后会保留已提交的堆页、xxl-job 的调度统计缓存也会增长，都属于预期行为；但 **45 分钟窗口太短，无法把"保留已提交页"与"真泄漏"区分开**。因此本条**不作为"无泄漏"的结论**，而是触发下面的**泄漏判定协议**——本轮的职责到此为止，判定交给服务器批按协议执行。
3. **6 容器全程 `OOMKilled=false`、`Restarts=0`**。
4. **未调整任何参数**：按指令，发现爬升应先报告而不是调参掩盖；本轮没有出现需要掩盖的爬升，参数保持目标上界值。

#### 1.6.4 泄漏判定协议（可判定版，取代"建议延长观察 ≥8 小时"）

上一轮只写了"建议观察 ≥8 小时"——那是**意图不是判据**：既没写采样频率、没写判定阈值，也没说数据怎么留。本节把它补成可执行协议。

| 要素 | 规定 |
| --- | --- |
| **采样频率** | 每 **5 分钟**一次，`docker stats --no-stream --format '{{.Name}}|{{.MemUsage}}'`，每条样本带时间戳 |
| **窗口要求** | **≥8 小时，且必须跨越一次日切**。理由：日切会触发编号 key 重建、SLA 扫描对当天数据的处理、日志滚动等只在跨日出现的行为；只覆盖一个工作时段会漏掉它们 |
| **数据可比性** | `docker stats` 的 **MEM USAGE 已扣除 page cache**，因此**可以跨时点直接比较**；不得用宿主 `free` 的 used 或 `docker inspect` 的原始 memory 统计替代（两者都含 cache，会随时间单向上涨，无法用来判断泄漏） |
| **判据 1（斜率）** | 每容器 RSS 对时间做线性回归，**斜率 ≤ 0.5 MiB/小时**（等价于 8 小时窗口内总增长 ≤4 MiB） |
| **判据 2（幅度）** | 窗口内**最大值 ≤ 该容器 `mem_limit` 的 70%**，留 30% 吸收峰值与内存碎片 |
| **判定** | **两条同时成立**才算"无泄漏"；任一不成立 → 判为**疑似泄漏或容量不足**，**停下报告**，不得调参数掩盖 |
| **留档要求** | 每次观察必须把（容器 / 窗口起止 / 样本数 / **斜率 MiB·h⁻¹** / 最大值 / 是否跨日切）写入服务器批报告。**没留斜率的观察不算完成**——否则下次只能靠"感觉变了没有" |
| **不通过时的处置顺序** | 先分类：**单容器持续增长**（查代码：对象/连接/线程未释放）vs **全容器同步增长**（查配置：缓存无界、连接池无上限、批量任务未限速）。**禁止先调 `mem_limit` 或堆**——那只会把问题推到 OOM 那天 |

#### 1.6.5 压测方法学：必须分离预热阶段

**规定**：压测**先跑 2 分钟预热，这部分数据全部丢弃**；从第 2 分钟起再统计 P50/P95/P99。预热阶段的数据**单独报告**（不混入正式统计），并写明丢弃了多少样本。

**第二条方法学（2026-09-25 补，与预热分离同等重要）：计时必须是"每请求自己完成"的时刻。**
压测脚本按"波"发请求（一波 = 并发数），收割任务时**不能**先 `WaitAll` 再统一停表——
那样同一波里每个样本拿到的都是**该波最慢请求**的耗时。实测代价：
改造前构建（桩延迟 3s、30 并发、真分布是"20 个 ≈3.0s + 10 个 ≈6.1s（等连接池）"）
会被记成**全部 6.15s**，P50 从 3.13s 被抬到 6.15s；异步路径的 P50 也从 96.2ms 被抬到 110.7ms。
正确做法是**按完成顺序收割**（或直接用 curl 的 `%{time_total}`）。
**为什么单列一条**：这个错误不会让任何请求失败，只会让所有百分位都变成"波最慢值"，
而且**两版脚本会因此产生不同口径**（bash 版一直是每请求计时）——属于"看起来一切正常"的错误。

**依据（本轮实测暴露的方法学缺陷，不是性能结论）**：本轮突发压测得到 P99 **167.7ms** / max **331.1ms**，但同期**后端 CPU 利用率只有 0.05 核**（0.24% × 20 核）。算力远远没用满却出现长尾，说明这些毛刺来自 **JIT 未预热、类加载、连接池首次建连、首次 GC** 等一次性成本——**当前数据无法区分"预热成本"与"真实问题"**。不分离预热，长尾数字既不能用来判断性能，也不能用来定位瓶颈。

**同一处的另一条限制（见 `docs/DECISIONS.md` D27）**：上文的 CPU 读数（0.17–0.24%）是 **20 核宿主**口径（≈0.05 核），**不可外推到 2 vCPU 目标机**——核数变化会改变同时改变占用率分母与瓶颈形态（排队、节流、GC 抢核）。**2 vCPU 的 CPU 结论必须在目标规格下重测**。

#### 1.6.6 服务器批实测（腾讯云轻量 4 vCPU / 4 GiB）

> 编号说明：本节按用户要求命名为"服务器批实测"，但 §1.6.4（泄漏判定协议）与 §1.6.5（压测方法学）
> 已被占用，故顺延为 §1.6.6。本节数据来自真实服务器，用于替换 §1.6.1–§1.6.3 的本机预演值。

**平台**：腾讯云轻量应用服务器 · AMD EPYC 7K62 ×4 · `MemTotal` **3.64 GiB** · `MemAvailable` **3.06 GiB** ·
随机 4K 读 **26.0k IOPS**、**p99 1745µs** · 系统盘 40G。

**4 容器（正式栈：mysql / redis / backend / frontend）**

| 时点 | mysql | backend | frontend | redis | 合计 | MemAvailable |
| --- | --- | --- | --- | --- | --- | --- |
| 稳态 | 372 MiB | 273 MiB | 4.3 MiB | 3.4 MiB | **653 MiB** | 2290 MiB |
| 8 小时后 | 374 MiB | 291 MiB | 4.5 MiB | 3.5 MiB | **673 MiB** | 2299 MiB |

**斜率判定（按 §1.6.4 协议）**：8 小时增长 **20.7 MiB → ≈2.6 MiB/h**，**低于 5 MiB/h 的斜率判据** →
**未发现泄漏**。判据二（窗口内最大值 ≤ 70% `mem_limit`）亦成立。

> **必须保留的限制**：本次只有**两个采样点**，**单调性未验证**——2.6 MiB/h 是端点斜率，不能区分"缓慢泄漏"
> 与"稳态波动"；且窗口未跨越日切（§1.6.4 要求 ≥8h 且含日切）。采样曲线见附录 `observe.csv`。
> 因此结论应表述为"**未发现泄漏迹象**"，而非"已排除泄漏"。

**6 容器旁证**：混合状态（含 rabbitmq / xxl-job-admin）实测 **1.03 GiB**，`MemAvailable` **1965 MiB**。

**系统底噪实测 ≈400 MiB**，其中约 **115 MiB 是腾讯云自带 agent（不可 kill）**——这部分是 §1.2
"系统底噪 300–400M"估算的实测落点，属**固定开销**，做容量规划时必须计入。

**结论（真机口径）**：

| 形态 | 实测占用 | MemAvailable | 余量判断 |
| --- | --- | --- | --- |
| 4 容器（正式栈） | 653–673 MiB + 底噪 ≈400 MiB | 2290 MiB | **> 2.2 GiB** |
| 6 容器（含 MQ 与调度中心） | ≈1.03 GiB + 底噪 ≈400 MiB | 1965 MiB | **> 1.9 GiB** |

**与 §1.6.2 的关系**：§1.6.2 的容量规划口径（按参数上界推算 ≈2.7 GiB 总量、余量 ≈1 GiB）
**偏保守，但方向正确**——真机实测比上界推算更宽松。**保留 §1.6.2 的上界口径用于规划**：
它算的是"堆被用满时的最坏情况"，而本次实测是"当前负载下的稳态"，两者用途不同（见 §1.6.2 方法学）。

#### 1.6.7 容器形态变化：4 → 5（P1 步骤 3 加入 RabbitMQ，2026-09-24）

**这是形态变化，不是"多起一个容器"**：§1.6.1–§1.6.6 的全部实测值都是 **4 容器**
（mysql / redis / backend / frontend）口径，本节起必须按 **5 容器**重算余量。

**本机 5 容器预演实测（目标上界参数：backend `-Xmx512m`、buffer pool 256M、rabbitmq mem_limit 512m；WSL2）**：

| 组件 | 实测 RSS | 容器上限 |
| --- | --- | --- |
| mysql（buffer pool 256M） | 461.2 MiB | 1g |
| rabbitmq（自建镜像 + 延迟插件，空载无积压） | 136.8 MiB | 512m |
| backend（`-Xmx512m`） | 261.5 MiB | 1g |
| frontend（nginx） | 16.3 MiB | 64m |
| redis | 5.2 MiB | 256m |
| **合计** | **≈881 MiB** | — |

**读数要点**：

1. 与 §1.6.6 的 4 容器服务器基线（653 MiB）相比，**增量 ≈228 MiB**；其中 RabbitMQ 单项 ≈137 MiB。
   §1.2 原估 180–260M 并标注"这是本表中最可疑的低估项"——**本轮实测低于估算，该标注可撤销**
   （撤销的前提是"空载无积压"；有堆积时 broker 内存会随 `vm_memory_high_watermark` 上升，见 §5.7）。
2. **本机数字不等于服务器数字**：WSL2 的系统底噪测不准（§1.6.2 方法学），所以"5 容器在 2C4G 上的余量"
   仍要由服务器 `free -m` + `docker stats` 给出；本表只能说明"加入 RabbitMQ 的增量有限"。
3. 在服务器批补测 5 容器之前，容量规划**继续用 §1.6.2 的上界口径**（≈2.7 GiB、余量 ≈1 GiB），
   不得用本节的轻载实测值反推可用余量。

#### 1.6.8 6 容器服务器实测（P2 步骤 1 加入 xxl-job-admin，2026-09-26）

**取数时刻**：**admin 启动后约 20 秒、整栈未热身**（因此下表是"刚起来"的读数，不是稳态；
与 §1.6.6 的"稳态/8 小时后"不同状态，**二者不可直接相减**）。

| 组件 | 实测用量 / 上限 | 说明 |
| --- | --- | --- |
| xxl-job-admin | **240.7 MiB / 512 MiB** | `-Xmx256m`（**heap 上限的生效方式见下面的教训 2**），未热身 |
| backend（`-Xmx512m`） | **311.8 MiB / 1 GiB** | 与 §1.6.6 的 4 容器口径同一参数 |
| mysql | **462.4 MiB / 1 GiB** | buffer pool 256M |
| rabbitmq | **124 MiB / 512 MiB** | 空载、无积压 |
| redis | **4.277 MiB / 256 MiB** | 仅会话/幂等键 |
| frontend | **4.508 MiB / 64 MiB** | nginx 静态资源 |
| **MemAvailable** | **1750 MiB** | 与 §1.6.6 的 4 容器值（约 2290 MiB）相比 **≈−540 MiB** |

`free -m` 原文：⏳ **待贴**（委托方本轮只给了 `MemAvailable 1750` 这个数；原文到位后补进本行——
**不代为转述**，因为 `free -m` 的每一列（total/used/free/buff-cache/available）都是判据的一部分）。

**读数要点**：
1. **容量规划仍按 §1.6.2 的上界口径**（≈2.7 GiB、余量 ≈1 GiB），本节是"当时剩多少"的读数，不是规划依据；
2. 与 §1.6.7 的 5 容器基线（服务器稳态 653 MiB）对照，**加入 admin 的增量 ≈240 MiB**——
   与 §1.2 原估的 admin 400–500M 相比偏低，但**两者口径不同**（原估是"按上限推算的量级"，这里是"未热身实测"），
   按 §1.6.1 的"三列不可混用"处理：规划看上限、现状看实测。

**四条教训/事实（每条都带判据，不写光秃秃的结论）**

1. **就绪判据禁止用固定 `sleep`**。判据：admin **冷启动 `22.9 seconds`（JVM 自身 `25.8 seconds` 量级）**，
   所以 `sleep 20` 之后去访问必然拿到连接失败 → **误判成"起不来"**，然后演变成"反复重启/反复排查"。
   正确做法：**轮询**（`docker compose logs` 里等 `Started XxlJobAdminApplication`，或反复请求 `/xxl-job-admin/toLogin` 直到 200）。
   > 与上一轮 `ConnectionResetError`（容器刚重建时 Docker 端口代理**先接后断**）**合并为同一条规则**：
   > **凡是"起了没起来"的判断，都不许用固定等待时间——必须轮询到成功或超时。**
   > 判据也从"我等了 N 秒"变成"看到某一行日志 / 某个 HTTP 状态"。

2. **堆上限要验到"命令行"——而且"出现在 argv 里"≠"生效"**。
   本镜像 entrypoint 是 `sh -c "java -jar $JAVA_OPTS /app.jar $PARAMS"`：**`$JAVA_OPTS` 落在 `-jar` 之后**，
   那个位置是**应用参数**、不是 JVM 选项。2026-09-26 实测把堆值放 `JAVA_OPTS` 时，
   `docker exec <c> sh -c 'cat /proc/[0-9]*/cmdline' | tr '\0' '\n'` 里**能看到 `-Xmx256m`**，
   但 JVM 用的仍是默认堆（4G 宿主上约 1G）——**"看到"只证明了它被传进去，没证明它被解析**。
   **查法（两条，缺一不可）**：
   · **不要查 `/proc/1/cmdline`**：PID 1 是 `sh`，它显示的是 `java -jar $JAVA_OPTS /app.jar $PARAMS` 这句**没展开的脚本**（本轮踩过）；
     要查 `cat /proc/[0-9]*/cmdline | tr '\0' '\n'`，看**真正的 java 子进程**；
   · **生效性判据**：改用 `JAVA_TOOL_OPTIONS`（JVM 启动器直接读取、与命令行位置无关）后，
     启动日志必须出现 `Picked up JAVA_TOOL_OPTIONS: -Xmx256m -XX:MaxMetaspaceSize=128m`；
     更强的一招是临时加 `-XX:+PrintCommandLineFlags`，日志里会出现
     `-XX:MaxHeapSize=268435456 -XX:MaxMetaspaceSize=134217728`（本机实测原文）——
     **这才是"上限真的生效"的证据**。compose 已按这个方式写（`JAVA_TOOL_OPTIONS`）。
   **换镜像/升版本必须复验**：entrypoint 的写法是镜像内部实现，不是契约。

   > **对照（同一件事的两种写法）**：本仓库**后端**镜像的 `Dockerfile` 是
   > `ENTRYPOINT ["sh", "-c", "java $JAVA_OPTS -jar app.jar"]`——**选项在 `-jar` 之前**，
   > 所以 backend 的 `-Xmx512m` 基线是**真生效**的；xxl-job 那份是 `java -jar $JAVA_OPTS /app.jar`，
   > **只差一个位置，结果完全相反**。这也是"基线值必须验生效、不能只看配置文件"的最好例子。

3. **容器内 Tomcat 绑的是 IPv6 双栈**：实测 `tcp4=0 / tcp6=1`（在容器内看 `/proc/net/tcp` 与 `/proc/net/tcp6`），
   宿主机仍能通过 docker-proxy 访问（`curl 127.0.0.1:8080/xxl-job-admin/toLogin` → 200）。
   **判据**：`/proc/net/tcp*` 里没有那条端口 ≠ 没在监听——**必须用一次真实请求（或 `docker port`/`ss -lntp`）来判定**。

4. **同一参数、不同状态不可比**：本机 admin 实测 **358.8 MiB** 与服务器 **240.7 MiB** 用的**都是 `-Xmx256m`**，
   差的是**取数时刻/负载**（本机是跑过一轮之后的读数，服务器是未热身的读数）。
   这是 §1.6.1 那句"三列不可混用"的**延伸**：不只"参数不同不可比"，**"参数相同但状态不同"也不可比**——
   要比就写明"同一参数 + 同一状态"（空载/稳态/峰值），否则差值只是在测"你什么时候看的"。

## 二、业务点论证（本轮核心）

### 2.0 判定标准：什么才叫"真的需要异步"

先立标准，避免"能上 MQ 就想上 MQ"。一个业务点要过三道关才算真正需要异步：

1. **同步会污染核心链路**：慢（外部 I/O、批量写）或不可靠（可能失败），把它的失败/延迟传导成核心业务失败。
2. **执行结果可以最终一致**：异步化后，短暂的不一致状态业务上可接受，且有兜底路径能收敛。
3. **丢失与重复的后果明确可控**：能说清丢一次、重一次分别造成什么，且有防线。

再过一道"M"关：**这件事需要跨进程、跨重启、跨实例的可靠性吗？** 不需要的话，线程池或进程内事件足够，引入 MQ 就是负债。

### 2.1 创建工单后的通知推送

**现状**：`WorkOrderServiceImpl.submitOrder()` 只插入工单与操作日志，**完全没有通知逻辑**。通知目前只出现在两处：驳回达上限升级（`rejectOrder` → `notificationService.sendToRole`）和 SLA 超时（`SlaEscalationScheduler`）。所以"提交后通知处理人/部门"是**新增需求**，不是改造已有逻辑。

> **已实现（P5 步骤 2，2026-09-25）**：本节论证的链路已按"提交事务只写 outbox、接收人解析放消费端"落地。
> **口径补充**：通知对象取**按角色广播（`HANDLER` 全体）**——这正是本节"一个校区 30–50 名处理人"的前提；
> **"按部门"不采用**（`t_work_order` 没有部门字段，按提交人部门解析属于发明规则）。
> 规则本身已登记为业务基线 `BUSINESS-SCOPE.md` F1-1 的「提交通知规则」表，**不新增功能条目**（功能总数仍 25）。

**为什么必须异步**：

- `NotificationServiceImpl.sendToRole()` 是"查出角色下所有用户 → 逐个 `insert`"的同步循环。一个校区若有 30–50 名处理人，就是 30–50 次单行插入，在同一个请求线程里串行执行，提交接口 RT 会随处理人数量线性上涨（30 人约 +60–150ms，且随表增长恶化）。
- 更要命的是它若被放进提交事务：任何一个通知插入失败（表锁、连接超时），用户的工单提交就整体回滚——把"旁路动作"的失败变成"核心业务"的失败。这是典型的错误耦合。
- 它还会与业务写争抢同一个 Hikari 连接池（`maximum-pool-size: 20`）：通知写入是 N 次插入，并发提交时连接池会被快速占满。

**消息丢失的后果**：处理人不知道池子里有新单，工单静默躺着。要注意 `sla_deadline` 是在提交时就计算好的，所以**丢失期间 SLA 计时照常流逝**——最坏情况是工单在被任何人看到之前就已经超时升级。可接受，因为 PENDING 池有列表轮询兜底，且 SLA 扫描会先一步告警。

**消息重复的后果**：同一处理人收到两条相同站内信。属于骚扰级，非数据错误，用去重键（见 5.2）即可抑制。

**是否真的需要 MQ**：**需要。** 理由不是吞吐，而是"持久化 + 可重试 + 进程重启不丢"——这三件事线程池给不了（线程池任务在应用重启时直接消失，且没有重试记录）。代价是多一条需要运维的投递链路，以及必须做去重。

### 2.2 AI 分类与摘要（Triage）

**现状（本方案里最该改的一处）**：`submitOrder()` 内部同步调用 `getTriageSafe()` → `orderTriageService.triage()` → `RestTemplate.postForEntity`，`llm.api.timeout: 5000`。触发条件是用户没填 `type` 或 `priority`。

**为什么必须异步**：

- **它把不可控的外部依赖放进了核心写事务**。LLM 供应商抖动、限流、网络抖动都是常态，每次失败或慢响应都直接变成用户提交工单变慢或超时。
- **事务持有连接 5 秒**。`@Transactional` 包着这次 HTTP 调用，意味着数据库连接被占用 5 秒。连接池上限 20，**只要 20 个用户同时不带类型提交，整个应用的所有数据库操作都将阻塞**。这不是理论风险，是当前配置决定的必然结果。
- **P99 从毫秒级劣化到秒级**。用户不填类型时必然等 5 秒，前端体验上等同于"提交卡住"。
- **时延随外部服务波动，无法通过扩容自愈**。

**消息丢失的后果**：工单保留兜底值（`TriageResult.fallback()` = `OTHER` / 普通），处理人拿到一张未分类的单，需要人工改派。业务可继续，属于**可接受的降级**——前提是工单必须已经落库（即"先落库、后修正"，见下）。

> **追加（P5 步骤 3，2026-09-25）**：这里说"保留兜底值"仍然成立（工单在 `PENDING` 期间显示的就是 `OTHER`/普通），
> 但有两点被本轮修正：① **`triage()` 不再把失败吞成兜底值**——它改为抛 `TriageUnavailableException`，
> 因为消费端必须能区分"AI 判成其他"与"这次根本没调通"（否则失败会被写成 `DONE`，见 D62 与 `INVARIANTS.md` I12）；
> ② 兜底值不会永远保留——重试阶梯走完仍未成功时，工单收口为 `triage_status='FAILED'`（界面显示"分类失败"），
> 而不是停在 `PENDING` 让"分类中"变成假终态。

**消息重复的后果**：重复消耗 token（真金白银），且**第二次结果可能覆盖第一次**。这里有一个必须点破的连锁风险：`sla_deadline` 是按 `type + priority` 查 `t_sla_config` 算出来的。如果异步 triage 把类型从 `OTHER` 改成 `REPAIR`，而 SLA 期限不重算，就会出现"**SLA 期限与该类型规定不符**"的静默数据错误。所以异步 triage 必须连带产生"重算 SLA + 记一条修正日志"的后续动作。

**是否真的需要 MQ**：**需要，但形态是"先落库后修正"**。提交时若缺少类型/优先级，用兜底值落库并标记 `triage_status=PENDING`，立即返回；消费者调用 LLM，成功后写回类型/优先级 + 重算 SLA + 记日志，失败按退避重试，超限则保持兜底值并告警。选 MQ 而非线程池的理由：外部服务失败率高，需要**可持久化的退避重试**，而重试次数与退避节奏在内存里无法跨重启保持。代价：triage 结果与工单类型存在窗口期不一致，列表/详情必须能表达"分类中"状态。

### 2.3 SLA 超时告警与自动升级

**现状**：`SlaEscalationScheduler` 每 5 分钟 `@Scheduled` 扫表（`findSlaExpired`，`LIMIT 200`），命中后用 Redis `SETNX sla_notified:{orderId}` 去重，然后直接同步调 `notificationService.sendToRole`。另有 `ReleaseTimeoutScheduler` 每 60s 扫 `status='ACCEPTED' AND updated_at <= now-30min` 做超时释放。

**为什么必须异步**：这条链路上没有"同步 vs 异步"的取舍（它本来就是后台任务），真正的问题是**纯 `@Scheduled` 作为主路径够不够**。四个具体缺陷：

1. **粒度抖动**：5 分钟周期意味着 SLA 到期到告警最多延迟 5 分钟；对 PENDING 状态的报修单，这 5 分钟可能就是投诉电话的来源。
2. **`LIMIT 200` 的积压恶化**：单周期只能处理 200 条。若某次批量导入或系统故障导致积压 1000 条超时工单，需要 5 个周期（25 分钟）才能通知完，而 SLA 仍在继续超时——告警本身变成了滞后指标。
3. **多实例不安全且无调度视图**：`@Scheduled` 在每个实例都触发。当前靠 Redis SETNX 与 SQL 乐观锁兜住正确性，但会产生无意义的重复扫描与日志噪音；且没人知道"上一次任务到底跑成功了没有"。
4. **同步发通知**：`sendToRole` 在扫描循环里同步执行，管理员多、积压多时会让单次扫描任务长时间运行，进一步拉长下一轮周期。

**消息丢失的后果**：**最严重的一类**。管理员不知道有工单超时 → 工单卡死 → 报修人反复催办/投诉。所以这条链路是"不可丢"等级，必须有兜底。

**消息重复的后果**：管理员重复收到同一告警。可用事件级去重键抑制（见 5.2）。要特别指出：**现存 `sla_notified:{orderId}` 键把"同一工单的第二次合法告警"也吞掉了**（例如先 SLA 超时告警、后驳回超限告警，orderId 相同但事件类型不同），这是漏发而非重发，详见 3.4。

**是否真的需要 MQ**：**需要，且是有明确工程价值的那一个点**，但要分清两类延迟：

- **固定延迟**（接单后到点检查释放，时限来自配置）：适合 MQ 延迟消息。它与 `ReleaseTimeoutScheduler` 的 30 分钟常量、Redis `order:accept_timeout:` 的 30 分钟 TTL 是同一业务参数的**三处重复定义**（见 3.4），应先收敛成一个配置。
- **动态 deadline**（`sla_deadline` 因人而异，取决于 type/priority 的 `finish_minutes`）：**不适合固定档位延迟消息**。剩余时间不固定，无法预设 TTL 档位；用"1 分钟轮询队列"的方式又会让每条消息被重复投递多次。用**调度（分钟级扫描）**更合适。

**结论分工**：固定延迟交 MQ，动态 deadline 交调度，两者都保留兜底扫描。这与 `TECHNICAL-PLAN.md` 3.3 节"主路径快且精准，兜底路径不漏"的思路一致。

### 2.4 状态变更的事件订阅

**现状**：没有事件机制。状态变更的直接副作用用两种方式处理：`OrderLogAspect` 在同一事务里同步写日志；跨事务的副作用用 `TransactionSynchronizationManager.registerSynchronization(...afterCommit)` 手工注册（`acceptOrder`、`assignOrder`、`rejectOrder` 的升级分支）。

**判断**：**当前不需要 MQ 级的事件总线**。理由是**没有真实的第三方订阅者**——没有 webhook 对接、没有开放 API、没有外部审计系统，所有"消费者"都在本进程内。此时引入跨进程事件总线是纯粹的架构负债（多一份消息契约要维护，却没有多一个消费者）。

**但有一个例外必须处理**：`afterCommit` + 发消息的组合是一个**双写漏洞**（dual-write）。`afterCommit` 回调只在事务提交后执行一次，如果：

- 进程在 commit 之后、send 之前崩溃（或重启/被驱逐/OOM Killer 杀掉），消息**永久丢失且没有任何记录**；
- send 抛异常，业务已经提交无法回滚，只能靠日志发现。

当前 Mock 实现下这个漏洞不可见（只打一行日志，几乎不会失败）。**接上真实 MQ 的第一天，它就会变成线上问题**。这正是 5.1 要解决的，也是本次改造里必须新增一张表的原因。

**丢失 / 重复后果**：丢失 → 释放检查或升级通知不发（前者有扫描兜底，后者不可接受）；重复 → 由消费端状态守卫（`WHERE status='ACCEPTED'` / `WHERE version=?`）吸收，无错误结果。

**结论**：进程内事件用 Spring `ApplicationEventPublisher` 即可（不引入 MQ）；跨事务的可靠投递用 outbox 表（见 5.1）。事件总线作为"未来对接外部系统"的预留点记录在案，现在不做。

### 2.5 统计报表生成

**现状**：`getStats()` 在请求线程内做 `SELECT status, COUNT(*) ... GROUP BY status`，`DEPT` 范围时先查部门用户列表再 `IN`。前端看板直接调这个接口。

**拆成两个需求看，结论完全不同**：

| 需求 | 结论 | 理由 |
| --- | --- | --- |
| 实时看板（今日各状态计数） | **不需要异步** | 有 `idx_status`，10⁵ 行量级下聚合是毫秒级；真正需要的是 30–60s 的结果缓存，属于性能优化而不是异步化 |
| 周期报表 / 导出（周报、月报、跨月同比、Excel 导出） | **需要异步，但只需要调度，不需要 MQ** | 跨月全表聚合会到秒级甚至十秒级，放在请求线程里必然超时；导出还会生成大文件占用内存 |

**为什么只需要调度**：报表任务天然是"触发一次、跑一次、写一个结果"的批作业，没有事件语义，也没有需要解耦的上游。用 xxl-job 定时触发 + 落结果表 + 通知下载链接即可。

**丢失 / 重复后果**：**最轻的一类**。报表可以随时重跑，重复生成按 `report_date` 唯一键覆盖即可，无副作用。所以它不应该被纳入高可靠性链路，也不该消耗 MQ 的复杂度预算。

### 2.6 历史数据归档

**现状**：完全没有归档机制。`t_work_order` 与 `t_work_order_log`（后者只有 `idx_order` / `idx_order_no` / `idx_created`，无归档字段）都是只增不减。

**为什么必须异步 + 为什么这是调度任务而不是消息**：

- 归档是"批量读 + 批量写 + 批量删"的 **IO 密集**操作，放在请求线程不可能；放在业务高峰会与在线查询争抢 IO。
- 单次大批量 `DELETE` 会产生大事务、长锁、binlog 膨胀，在 2C4G 上足以把在线服务拖垮。必须**分批 + 限速**（每批 N 行，批间 sleep）。
- 必须**可中断可续跑**：任务被重启/超时中断后，要能从断点继续而不是重头再来。这要求有游标或水位线，而不是靠内存里的下标。

**丢失 / 重复后果**：归档任务重复执行最多重复搬运，用 `order_id` + 水位线约束保证幂等；丢失只是"这次没归档"，下个周期继续。**对可靠性要求最低，但对"可重跑 + 限速 + 可观测"要求最高**。

> **限定（2026-09-27，P6 步骤 1 落地时）**：上面这句里的"必须可中断可续跑、要有水位线"针对的是**归档搬运**
> （把老工单搬进归档表，读一次写一次）；**归档删除**（按保留期清理 `t_consume_record` / `t_message_retry` /
> `t_event_outbox` 的 SENT 行）**不需要水位线**——删除幂等且可重放，"时间谓词 + 分片"每轮重新筛即可，
> 详见 §5.6 的反转注与 D70。

**是否真的需要 MQ**：**不需要。** 本质是定时批作业，xxl-job 的分片广播 + 失败重试正好匹配；用 MQ 反而要自己实现"任务完成回调 + 分片协调"。

### 2.7 失败补偿重试

**先说结论**：**"补偿"不应该被设计成一个独立业务模块，它应该是消息中间件与调度框架的机制**。真正需要在业务侧落地的只有两处：

1. **生产端补偿**：outbox 表中 `status=PENDING` 或投递失败的消息，由独立投递任务重投（5.1）。
2. **消费端补偿**：消费失败并超出退避上限的消息，进死信队列 / 停车队列，由定时任务重放或人工介入（5.5）。

**为什么必须异步**：补偿动作的触发时机与业务请求无关（可能几分钟后、几小时后），不能在请求线程里等待。

**丢失 / 重复后果**：补偿链路本身不丢（outbox 有记录、死信有留存）；重复重投由消费端幂等键吸收。所以**补偿重试的价值完全依赖幂等设计**——没有幂等键的重试机制不是可靠性，是放大器。

**是否真的需要 MQ**：重投动作本身只需要调度；但"重投到哪"需要 MQ。两者配合，不需要额外的补偿微服务或分布式事务框架。

### 2.8 明确"不需要"的场景（含理由）

| 场景 | 结论 | 理由（代价视角） |
| --- | --- | --- |
| **抢单**（`acceptOrder`） | **不需要，且绝对不能异步** | 这是强一致竞争点，靠单条原子 SQL（`WHERE assignee_id IS NULL AND status='PENDING'`）+ 乐观锁解决。改成消息驱动会引入"双人接单/重复接单"，把确定性竞争变成最终一致，代价远大于收益 |
| **状态机流转本身**（start / complete / approve / reject / assign / takeover / close） | **不需要** | 状态流转要与日志、乐观锁校验原子提交。异步化会把一个原子事务拆成两个不确定的步骤，收益为零、风险为正 |
| **操作日志写入**（`OrderLogAspect`） | **不需要** | 日志必须与状态变更同事务。异步写会产生"状态变了但日志没写"的审计空洞，而工单系统的日志就是争议时的证据。代价是事务时长略增，可接受 |
| **通知的读取 / 已读 / 未读数** | **不需要** | 纯读路径，走 `idx_user_read` 索引即可；要优化就加缓存，与异步无关 |
| **权限与角色变更后的缓存刷新** | **不需要（但需要的是缓存，不是异步）** | `getRoleCodes()` 目前每次请求都查 `t_user_role` + `t_role` 两趟库。这是真实的性能问题，解法是本地/Redis 缓存 + 变更时失效，属于同步优化范畴 |
| **用户注册 / 登录** | **不需要** | 低频写，且用户需要立即知道结果 |
| **分布式事务（Seata 等）** | **不需要** | 本项目没有跨服务/跨库写，所有一致性都在单库事务内。引入是纯过度设计 |
| **全文检索（ES 等）** | **不需要** | 搜索走 `order_no LIKE` + 状态/人员过滤，10⁵ 行量级完全够用。ES 在 4G 机器上会额外吃 1G+ 内存，并带来索引同步复杂度 |
| **实时看板** | **不需要** | 见 2.5，需要的是缓存而非异步 |
| **短信/邮件/企业微信等外部通知渠道** | **未来需要，现在不做** | `NotifyChannel` 已是策略模式扩展点。一旦接入外部渠道（有超时、限流、失败率），**必须走 MQ 而不是在事务里同步调用第三方 API**；现在没有渠道，先不建链路 |

### 2.9 汇总表

| 业务点 | 需要异步 | 需要 MQ | 可靠性等级 | 关键理由 |
| --- | --- | --- | --- | --- |
| 创建工单后通知推送 | 是 | **是** | 不丢优先（有轮询兜底） | 通知循环写入不能绑进提交事务 |
| AI 分类与摘要 | 是 | **是** | 可降级（兜底值） | 5s 外部调用持有 DB 连接，是当前最大风险 |
| SLA 超时告警与升级 | 是 | **部分**（固定延迟用 MQ，动态 deadline 用调度） | **不可丢** | 丢失直接导致工单卡死与投诉 |
| 状态变更事件订阅 | 进程内即可 | **否**（但要 outbox 保证跨事务投递） | 视事件而定 | 无第三方订阅者；双写漏洞必须修 |
| 统计报表 | 周期报表是 | **否** | 可重跑 | 批作业语义，不是事件 |
| 历史数据归档 | 是 | **否** | 可重跑 | IO 密集 + 需要限速与断点续跑 |
| 失败补偿重试 | 是 | 是（重投通道） | 高 | 是机制不是业务模块，价值依赖幂等 |
| 抢单 / 状态流转 / 操作日志 / 实时看板 | **否** | **否** | — | 见 2.8 |

**关于 outbox 与 MQ 的关系（避免误读）**：outbox 解决的是"业务已提交、消息还没发出去"这个跨事务窗口，**它依赖 MQ 完成最终投递**；不依赖 MQ 的只有**"何时发起补投"**这一件事（由调度触发）。换句话说：**outbox 是发件箱，不是邮局。** 若没有 MQ，outbox 里的记录只能靠轮询数据库消费，那属于 §4.2 记录的"不引入 MQ"的替代方案，不是本方案的主路径。

---

## 三、现有占位实现评审

### 3.1 现状清单（代码级）

| 位置 | 现状 |
| --- | --- |
| `service/MessagePublishService.java` | 两个方法：`sendReleaseCheck(Long orderId)`、`sendSlaEscalation(Long orderId)`，返回 `void`，无消息体、无延迟参数、无事件标识 |
| `service/impl/MockMessagePublishServiceImpl.java` | 两个方法各打一行 `log.info("[MOCK-MQ] ...")` |
| `service/impl/WorkOrderServiceImpl.java` | `acceptOrder` / `assignOrder` / `rejectOrder`(升级分支) 通过 `TransactionSynchronizationManager` 在 `afterCommit` 中调用发布接口 |
| `scheduler/ReleaseTimeoutScheduler.java` | `@Scheduled(fixedRate=60_000)`，扫 `ACCEPTED` 且 `updated_at <= now-30min`（**P1 步骤 5 起改为 JOIN `t_sla_config.accept_minutes`**），逐条 `releaseOrder` |
| `scheduler/SlaEscalationScheduler.java` | `@Scheduled(fixedRate=300_000)`，`findSlaExpired(LIMIT 200)`，Redis `SETNX sla_notified:{orderId}`（TTL 24h）去重，随后**直接同步** `notificationService.sendToRole("SYS_ADMIN", ...)` |
| `common/aop/OrderLogAspect.java` | 环绕 `@OrderAction` 同步写日志；无登录上下文时 `operatorId` 降级为 0 |
| `application.yml` | 已配置 `spring.rabbitmq.*`（`acknowledge-mode: manual`、`prefetch: 10`）与 `xxl.job.*`，但**代码中没有任何 RabbitMQ Bean / 监听器 / XxlJob 注解** |
| `pom.xml` | `spring-boot-starter-amqp`、`xxl-job-core` 2.4.0 均在依赖中 |
| `deploy/docker-compose.yml` | 只有 mysql / redis / backend / frontend，注释明确写了"不含 RabbitMQ / XXL-Job" |

### 3.2 原接口抽象是否适合真实 MQ

**结论：分层方向是对的，接口粒度不对。**

对的部分——`MessagePublishService` 让 `WorkOrderServiceImpl` 只依赖接口、不感知底层实现，这个解耦必须保留（`RABBITMQ-MIGRATION.md` 的方案 A"只换 Bean 实现"也成立）。

不对的部分——**接口把"投递"当成了唯一语义，丢掉了投递可靠性需要的全部信息**：

1. **只有 `orderId`，没有事件身份**。消费者无法区分"同一条消息被重投"与"业务上真实发生的第二次同类事件"。这不是理论问题：一张工单可以 `PENDING → ACCEPTED → RELEASED → ACCEPTED → RELEASED`，两次 `sendReleaseCheck(orderId)` 的 payload 完全相同，但业务上是两次合法事件。
2. **`sendSlaEscalation` 语义过载**。它同时被 `SlaEscalationScheduler`（真·SLA 超时）和 `WorkOrderServiceImpl.rejectOrder`（驳回次数达上限）调用——两类完全不同的业务事件共用一条通道，消费者无法区分，通知文案只能靠调度器自己拼（现状正是如此）。
3. **没有延迟参数**（P1 步骤 5 前）。30 分钟曾硬编码在实现里，同时另有两处重复定义（见 3.4）。
4. **返回 `void`**。调用方无法知道是否发送成功。配合 `afterCommit` 使用，等于"失败静默"，而 `afterCommit` 里抛异常既不能回滚业务，也只会污染日志。

**需要的重新设计方向**（不在本轮实现）：

- 接口改为事件驱动：`publish(OrderEvent event)`，`OrderEvent` 至少含 `eventId`、`eventType`、`aggregateId`、`aggregateVersion`、`occurredAt`、`payload`。
- 按可靠性分层：`ReleaseCheck`（可丢，有兜底）与 `Escalation`（不可丢）走**不同队列与不同重试策略**，不能共用一个 `void sendXxx`。
- 投递失败必须可观测：要么抛受检异常，要么写 outbox（推荐后者，见 5.1）。

### 3.3 缺失语义清单

| 语义 | 现状 | 缺失后果 | 改造要求 |
| --- | --- | --- | --- |
| **幂等键** | 无（仅消费端一个粗粒度的 `sla_notified:{orderId}`） | 重投被误判为重复而漏发；或重复发通知骚扰管理员 | 引入 `eventId = {aggregate}:{id}:{version}:{eventType}`；消费端按 `eventId + consumer` 去重 |
| **重试** | 无（Mock 不会失败，看不出问题） | 真实 MQ 下瞬时故障直接丢消息 | 消费失败分级：可重试退避重投，不可重试进死信 |
| **死信** | 无 | 失败消息要么被无限重投堵死队列，要么被静默丢弃 | 配置 DLX + 停车队列 + 告警 |
| **顺序保证** | 无契约 | 乱序投递时消费者不知道能否安全并发 | 明确"并发消费 + 状态守卫"（本项目适用，见 5.4） |
| **延迟** | 硬编码 30min，且三处重复常量（**P1 步骤 5 已收敛到 `accept_minutes`**） | 改一个参数要改三处；动态 deadline 无法用固定档位表达 | 固定延迟走 MQ 延迟消息（档位可配），动态 deadline 走调度 |
| **消息体** | 只有 `orderId`（瘦消息） | 消费者每次回查 DB；但也避免了消息体与 DB 不一致 | **保留瘦消息**并写进契约；代价是每次消费一次 DB 读，换来消息永不过期失真 |
| **发布确认** | 未启用 | broker 未收到消息时生产端不知情 | 开启 `publisher-confirm-type: correlated` + `publisher-returns`，并要求 outbox 状态以 confirm 为准 |
| **消费确认语义** | `acknowledge-mode: manual` 已配，但无监听器 | 无法决定 ACK/NACK；业务方法返回值不足以支撑判定 | 明确 ACK/NACK 契约（见 3.4 第 5 条） |
| **可观测性** | 只有日志 | 出问题无法回答"消息到哪了、重试几次、谁在消费" | 关键指标：投递成功/失败数、队列深度、死信数、消费耗时、重试次数 |

### 3.4 具体漂移点（改造前必须先对齐）

1. **`SlaEscalationScheduler` 的"发 MQ"是死代码，且迁移后会双发。**
   调度器里同时有 `messagePublishService.sendSlaEscalation(orderId)`（Mock，只打日志）和 `notificationService.sendToRole("SYS_ADMIN", ...)`（真实发送）。而 `RABBITMQ-MIGRATION.md` 的消费者实现里，`SlaEscalationListener` 收到消息后**也会发一次通知**。按该文档切换实现后，同一事件会被通知两次（调度器一次 + 消费者一次）。要么删掉调度器里的直调，要么让调度器只发消息。**迁移前必须先定权威路径。**

2. **30 分钟这个参数曾在三处独立定义**（**P1 步骤 5 已收敛：时限统一来自 `t_sla_config.accept_minutes`；Redis 那个键判定为死设计并移除，见 D48**）：`ReleaseTimeoutScheduler` 的 `minusMinutes(30)`、`WorkOrderServiceImpl` 写入 Redis 的 `Duration.ofMinutes(30)`、`RABBITMQ-MIGRATION.md` 里的 `setDelay(30*60*1000)`。任何一处单独调整都会造成行为不一致（例如延迟消息 20 分钟触发而扫描器 30 分钟才兜底，工单会被提前释放）。**解法已定稿，不再是未决项**：见 `BUSINESS-SCOPE.md` F4-1 的「`accept_minutes` 归一」三件事——a) 释放时限读取 `t_sla_config.accept_minutes`（按工单的 `type+priority`）；b) 兜底扫描 SQL 由 `updated_at <= now-30min` 改为按配置表判断；c) Redis 接单超时标记的 TTL 与延迟消息的 `delay` 取同一配置值。本文件不再重复展开，只在实施时引用该结论。

3. **`sla_notified:{orderId}` 的键粒度错误**，有三个独立问题：
   - 同一工单的两类不同事件（SLA 超时、驳回超限）会互相吞掉 → **漏发**；
   - 24h TTL 与注释"每张工单只发一次"自相矛盾：24h 后同一张未完结工单会再次告警；
   - Redis 写键失败时 `acquired = Boolean.TRUE`（fail-open），即在 Redis 故障期**主动放弃去重**，MQ 重投会放大成重复告警。
   正确做法是按事件去重（`{orderId}:{triggerType}:{eventVersion}`），并明确 fail-open 还是 fail-closed（SLA 场景建议 fail-open 但落库补记）。
   **`eventVersion` 的递增规则由 `BUSINESS-SCOPE.md` H1 的分级催办政策决定**（不再是"同一工单一个键"）：首次超时告警记 `v1`，此后该工单每满 24 小时仍未完结则告警一次并把 `eventVersion` 加 1（`v2`、`v3`…），工单完结（`CLOSED` / `RELEASED`）后停止递增。这样"同一工单的第二次合法告警"（版本已递增，必须发出）与"重复投递的同一条消息"（版本相同，必须去重）才同时成立。详见 5.2 节的递增表。

4. **Redis 淘汰策略 `allkeys-lru` 与业务键冲突**（见 1.5）。这是配置层面的正确性风险，优先级高于任何架构改造。

5. **`releaseOrder()` 的返回语义不足以支撑 ACK/NACK。**
   `WorkOrderServiceImpl.releaseOrder()` 在工单不存在时抛 `BizException`，在 SQL 影响 0 行时静默 `return`。消费者无法区分三种情况：真的释放成功、因状态已变更而正常跳过、以及内部出错。`RABBITMQ-MIGRATION.md` 的消费者因此只能"无论如何都 ACK"。该策略对本场景是对的（有兜底扫描），但**必须把"跳过"变成显式的三态返回**（`RELEASED` / `SKIPPED` / `ERROR`），否则将来任何状态守卫改动都会静默变成丢消息。

6. **`OrderLogAspect` 在消息消费线程里会把操作人记为 0**（`StpUtil.getLoginIdDefaultNull()` 降级）。现有降级逻辑是对的，但后果是"由消息触发的状态变更"在审计里全部是系统操作，无法区分是哪个消费者、哪次重投。需要显式传入系统操作人标识与消息 traceId。

7. **RabbitMQ 的启动耦合被低估。** `application.yml` 已配 `spring.rabbitmq.*`，但因为没有监听器/配置类，当前能正常启动（懒连接）。一旦引入 `@RabbitListener`，`ConnectionFactory` 与监听容器会在启动时连接 broker，**"没有 MQ 也能启动"的部署方式会被破坏**。必须用 `@Profile` / `@ConditionalOnProperty` 做开关，并保留 Mock 实现作为本地与 CI 的默认。

8. **`spring-boot-starter-amqp` 的配置样例存在自相矛盾。** `application.yml` 配了 `acknowledge-mode: manual`，`RABBITMQ-MIGRATION.md` 又建议加 `listener.simple.retry.enabled: true` 与 `max-attempts: 3`。手动 ACK 与自动重试叠加时，"重试发生在 listener 内部、最终失败后消息去哪"取决于 `MessageRecoverer` 与 `default-requeue-rejected` 的组合，**必须显式配置并验证，不能照抄**。另外必须显式设 `default-requeue-rejected: false`，否则消费异常的消息会被立刻重回队列形成热循环——这是最常见的"MQ 堵死"原因。

9. **两个调度器都没有分布式锁。** `ReleaseTimeoutScheduler` 靠 SQL 乐观锁天然幂等（多实例重复扫描不产生错误结果，只有无效开销与日志噪音）；`SlaEscalationScheduler` 靠 Redis SETNX（因第 3 条而会误吞）。迁移到 xxl-job 后由调度中心保证"一次触发一个执行器"。

10. **归档、报表、outbox 相关的表与任务完全不存在**；`t_work_order_log` 有 `idx_created`，可作为增量归档的时间水位依据，但目前没有任何使用方案。

### 3.5 改动清单

| 类别 | 对象 | 现状 → 目标 | 优先级 |
| --- | --- | --- | --- |
| 接口 | `MessagePublishService` | `void sendXxx(orderId)` → `publish(OrderEvent)`，含 eventId / eventType / version | P1 |
| 实现 | `MockMessagePublishServiceImpl` | 增加 `@Profile("!prod")` 或 `@ConditionalOnProperty`，保留为本地/CI 默认 | P1 |
| 新增 | `RabbitMQPublishServiceImpl` + `RabbitMQConfig` | 交换机/队列/绑定 + `publisher-confirm` + `publisher-returns` | P1 |
| 新增 | `OrderReleaseListener` / `EscalationListener` | 手动 ACK + 三态结果判定 + 幂等守卫 | P1 / P4 |
| 新增 | outbox 表 + 投递任务 | 把 `afterCommit` 直发改为"同事务写 outbox + 独立投递" | **P1**（v2 把原 P3 并入 P1） |
| 新增 | 消费去重表 | `UNIQUE(event_id, consumer)`，与业务写同事务 | P4 |
| 新增 | DLX + 重试表 + 重放任务 | 消费失败分级处理 | P4 |
| 改造 | `ReleaseTimeoutScheduler` / `SlaEscalationScheduler` | 迁移为 xxl-job 任务，明确阻塞策略与执行日志 | P2 |
| 改造 | `WorkOrderServiceImpl.submitOrder` | triage 改异步；新增提交通知；补 SLA 重算路径 | P5 |
| 改造 | `SlaEscalationScheduler` 通知路径 | 删除直调或删除发消息，二者只留一条 | P2 |
| 约束 | 30 分钟等业务常量 | 收敛为单一配置项 | P0 / P1 |
| 配置 | Redis 淘汰策略、RabbitMQ 内存水位、各容器 `mem_limit` | 见 1.4 / 1.5 | P0 |
| 新增 | 归档任务、报表任务 | 分片 + 水位线 + 幂等键 | P6 |

---

## 四、技术选型

### 4.1 消息队列：RabbitMQ vs Kafka vs RocketMQ

**结论：RabbitMQ。**

| 维度 | RabbitMQ | Kafka | RocketMQ |
| --- | --- | --- | --- |
| 单机内存（本场景形态） | 180–260M | 单 broker 通常 ≥1G（JVM + page cache 期望值），KRaft 另需元数据开销 | ≥1.5–2G（NameServer + Broker 两个进程） |
| 延迟消息 | 社区插件 `rabbitmq_delayed_message_exchange` 支持任意延迟；或用 TTL+DLX | **无原生支持**，需外部调度或时间轮 | 4.x 固定 18 档（30 分钟恰在档内，但动态 deadline 不在）；5.x 支持任意延迟 |
| 路由灵活度 | 交换机类型丰富，按 routing key 精确分发 | 只有 topic/partition，语义偏流处理 | topic + tag，较灵活 |
| 吞吐 | 万级 msg/s（远超本项目 ≤10 msg/s） | 十万级+ | 十万级+ |
| 消息回放 | 不支持（消费即删） | 原生支持，是核心优势 | 支持 |
| 运维复杂度 | 中（单容器 + 管理台） | 高（分区/副本/再平衡/磁盘规划） | 高（双进程 + 多组件） |
| 4G 内可行性 | **可行**（约 0.2G） | 勉强（单 broker 就吃掉 1/3 内存，且磁盘要求高） | **不可行**（两进程叠加后与 MySQL/后端/admin 争抢，无余量） |

**为什么选它**：本项目需求是"低吞吐 + 高可靠 + 多档延迟 + 灵活路由 + 内存受限"。RabbitMQ 在前四项都够用，第五项是唯一可行项；Kafka 的优势（回放、海量吞吐、流处理）本项目一项都用不上——**为用不到的能力付内存与运维成本是负收益**。

**替代方案**：若将来需要"事件回放、审计重放、把工单事件当数据流做分析"，才有引入 Kafka 的依据；届时应是独立机器或独立集群，而不是塞进这台 4G。

**它的代价（必须接受的三点）**：

1. **吞吐上限低**：单队列单节点万级 msg/s。本项目 ≤10 msg/s，余量充足，但这条边界要写进团队认知。
2. **延迟消息依赖社区插件**：插件独立分发，镜像需自备或启用；延迟消息存放在交换机内部，**不受队列长度与队列积压指标的覆盖**，堆积时难以观测，需单独监控；集群/仲裁队列场景下行为有限制（本项目单机单节点，风险可控）。
3. **管理台与 Erlang 运行时占用固定内存**，空载也要 180M+，比 Redis 重得多。

### 4.2 诚实的替代方案：不引入 MQ，只做 outbox + 调度

必须写出来，否则选型不完整：**本项目的量级下，不用 MQ 也能实现全部需求**——用 MySQL 的 `t_event_outbox` 表当队列，xxl-job 每分钟扫描 `status=PENDING AND next_retry_at<=now()` 投递并打标。

- **能达成**：不丢（outbox 有记录）、不重（消费端幂等键）、可重试（退避字段）、可观测（表就是视图）。
- **代价**：延迟精度受限于扫描周期（分钟级）；DB 承担队列职责，轮询带来固定查询开销；消费并发要靠应用层控制，无法用 prefetch 天然限流。
- **为什么最终仍选 RabbitMQ**：本项目明确要求企业级实现与演示价值，且延迟精度（30 分钟释放）与"避免把数据库当队列轮询"是真实收益；同时 xxl-job + outbox 会保留为**兜底路径**，两者不互斥。
- **对团队的诚实提示**：若最终发现 RabbitMQ 的运维成本（插件、水位、镜像）超过收益，退回"outbox + 调度"同样满足业务需求，不需要推翻本方案其他部分——**因为 5.1 的 outbox 是共同基础**。

### 4.3 调度框架：XXL-Job vs PowerJob vs 自建集群调度

**结论：XXL-Job 2.4.x。**

| 维度 | XXL-Job 2.4.x | PowerJob | 自建（`@Scheduled` + ShedLock/Redis 锁） |
| --- | --- | --- | --- |
| 额外内存 | admin 单 jar ≈ 0.32–0.4G + 独立 `xxl_job` 库 | server 更重；存储层传统以 MongoDB 为默认，4.x 虽支持 MySQL，但组件更多、占用更高 | 0（复用现有应用） |
| 依赖现状 | `pom.xml` 已声明 2.4.0，代码零使用 | 需新增依赖与部署 | 无需新增 |
| 能力 | 执行器注册、失败重试、阻塞策略、分片广播、执行日志、手动触发、告警 | 更强：工作流、MapReduce、任意延迟、容器化执行、更强的分片与可观测 | 需自己实现全部运维能力（日志、重试、手动触发、分片、超时） |
| 4G 内可行性 | 可行（余量内） | 紧张（admin + 存储层叠加后余量不足） | 最省，但功能全靠自建 |

**为什么选它**：`pom.xml` 已声明依赖（迁移不引入新技术栈）、admin 是单个 Spring Boot jar 可塞进 4G、有**手动触发与执行日志**（对演示与排障是刚需）、**分片广播**正好匹配归档任务、失败重试与阻塞策略是现成的运营能力。零学习成本在本项目里有实际价值。

**替代方案**：PowerJob——若将来需要工作流编排（如"归档 → 校验 → 通知"的多步依赖），或需要在大规模执行器上做 MapReduce，PowerJob 更匹配；代价是组件更重，在 2C4G 上缺少余量。

**自建方案**：`@Scheduled` + ShedLock 能解决多实例互斥且零新增组件，**适合"只要定时，不要运维"**的场景。但本项目要的是企业级实现，需要执行记录、失败可见、可手动重跑，这些自建成本高且容易做残。**结论：不选自建，但保留 `@Scheduled` 作为"admin 宕机时的降级兜底开关"（见 5.6）。**

**它的代价（三点）**：

1. **admin 单点**：xxl-job-admin 挂了，所有定时任务停止触发。4G 下不做 admin 高可用，只能靠监控心跳 + 关键任务最后执行时间告警来缩短 MTTR。
2. **多一个 Spring Boot 进程**：约 +0.4G 常驻内存与一份独立数据库 schema（`xxl_job` 库，可与业务库同实例）。
3. **调度精度与超时策略需要显式配置**：任务超时、阻塞策略（丢弃后续 / 覆盖之前 / 串行执行）默认值不一定符合预期，必须显式指定，否则会出现"上一轮还没跑完，下一轮又进来"的重叠执行。

### 4.4 边界：谁负责什么

| 职责 | 承载者 | 理由 |
| --- | --- | --- |
| 事件驱动、延迟触发、可靠投递、重试退避 | **RabbitMQ（+ outbox）** | 事件语义 + 需要跨重启可靠 |
| 定时批量、补偿扫描、归档、报表 | **XXL-Job** | 批作业语义 + 需要执行日志与分片 |
| 兜底扫描（防消息丢失） | **XXL-Job** | 主路径之外的"不漏"保证 |
| 进程内旁路动作（未来扩展） | Spring `ApplicationEventPublisher` | 无需跨进程，不引入额外中间件 |

**反模式警告**：不要把"定时扫描"和"消息投递"写成两条互不知情的路径（现状正是如此——调度器既发消息又直接发通知）。**每类事件只能有一个权威生产点**，兜底路径只负责"补漏"，不负责"再发一次"。

---

## 五、关键设计难点

### 5.1 生产端如何保证消息不丢：outbox 优先

**问题定义**：`afterCommit → convertAndSend` 存在双写窗口。业务提交与消息投递必须二选一失败，否则一定会出现"业务成功、消息丢失"。

**方案（推荐）：Transactional Outbox**

```
业务事务 {
    UPDATE t_work_order ...            -- 业务写
    INSERT INTO t_work_order_log ...   -- 审计写
    INSERT INTO t_event_outbox(...)    -- 事件写，与上面同一事务
}
COMMIT
        ↓
独立投递任务（xxl-job，5~10s 一次，或常驻线程）
        ↓
SELECT * FROM t_event_outbox
 WHERE status='PENDING' AND next_retry_at <= NOW()
 ORDER BY id LIMIT 100
        ↓  逐条 convertAndSend + publisher-confirm
        ↓  confirm=ack → status='SENT'；confirm=nack/超时 → retry_count++, next_retry_at=退避
```

**关键设计点**：

- `event_id` 全局唯一（建议 `order:123:v7:RELEASE_CHECK`），`UNIQUE(event_id)` 约束防止重复写 outbox。
- **只有 publisher-confirm 返回 ack 才标记 `SENT`**；仅"`convertAndSend` 没抛异常"不能证明 broker 已接收。
- 投递任务本身必须**多实例安全**：用 `SELECT ... FOR UPDATE SKIP LOCKED` 或状态抢占（`UPDATE ... SET status='SENDING', owner=? WHERE id=? AND status='PENDING'`），避免两个实例同时投递同一条。
- `SENT` 记录不能永久堆积：由归档任务按 `sent_at` 定期清理（例如保留 7 天），这也解释了为什么归档任务需要存在。

**代价**：每个事件多一次 DB 写（约 +1–2ms）与一张持续增长的表；投递延迟增加一个扫描周期（5–10s）。**对 30 分钟延迟释放与 SLA 告警而言，这 10 秒完全可接受**；对"要求 100ms 内送达"的事件则不适用——本项目没有这类事件。

**替代方案（若不接受 outbox）**：`afterCommit` 发送 + 失败落 `t_message_retry` 表 + 定时补偿。代价：仍存在"进程在 commit 后、写入失败记录前崩溃"的窗口，只是窗口更小。**不推荐，因为本项目已经买了 MQ，可靠投递的边际成本只剩一张表。**

### 5.2 消费端幂等与去重键设计

**核心原则：幂等键必须是"业务事件"维度，不能是"业务实体"维度。**

```
eventId = {aggregate}:{aggregateId}:{version}:{eventType}
例：order:123:v7:RELEASE_CHECK      ← version 让两次合法事件天然不同
    order:123:v2:SLA_ESCALATION
    order:123:v9:REJECT_LIMIT_ESCALATION
```

为什么必须带 `version`：一张工单可以被多次接单、多次超时释放。若只用 `orderId` 去重（如现状的 `sla_notified:{orderId}`），第二次合法事件会被当成重复消息丢弃——**这是漏发，比重复更危险，因为它静默**。

**告警类事件的 `version` 递增规则（与 `BUSINESS-SCOPE.md` 的 H1 分级催办政策对齐）**：

| 时刻 | 事件 | `eventId` | 期望行为 | 判定依据 |
| --- | --- | --- | --- | --- |
| T0 工单首次 SLA 超时 | 首次告警 | `order:123:v1:SLA_ESCALATION` | 发出 | 版本不同，属新事件 |
| T0（MQ 重投同一条消息） | 重复投递 | `order:123:v1:SLA_ESCALATION` | **去重，不重发** | `eventId` 完全相同 |
| T0+24h 仍未完结 | 第二次催办 | `order:123:v2:SLA_ESCALATION` | **发出** | 版本已递增，属同一工单的第二次合法告警 |
| T0+48h 仍未完结 | 第三次催办 | `order:123:v3:SLA_ESCALATION` | 发出 | 同上，每满 24 小时递增一次 |
| 工单进入 `CLOSED` / `RELEASED` | 停止 | — | 不再产生新版本 | H1：完结即停 |

**递增的实现要求**：`eventVersion` 由生产端在生成事件时确定并写进 outbox（同一事务），消费端不推导版本——否则重投时消费端可能算出不同的版本，去重就会失效。工单当前的 `version` 字段（乐观锁）**不能**直接用作 `eventVersion`：它随每次状态更新递增，与"每满 24 小时一次"的节奏无关。

**去重存储的选择**：

| 方案 | 优点 | 代价 | 结论 |
| --- | --- | --- | --- |
| MySQL 去重表 `t_consume_record(event_id, consumer, consumed_at)`，`UNIQUE(event_id, consumer)` | 强一致、跨重启有效、可审计、可与业务写同事务 | 每次消费多一次插入；表持续增长需归档 | **推荐为主 → P4 步骤 1 已实现**（保留 30 天，见 D52） |
| Redis `SETNX` + TTL | 快，TTL 自动清理 | 淘汰/故障会导致去重失效；TTL 到期后同一事件可再次被处理 | 仅作快速预判层（可选） |

**与业务写同事务是实现要点**：`INSERT INTO t_consume_record` 与业务 `UPDATE` 必须在同一事务内提交。若先去重再执行业务、两者不同事务，一旦业务失败，去重记录已存在，重试会被永久跳过——**用去重键把消息"静默吃掉"，是比重复更严重的故障**。

**具体到本项目的三类消费**：

| 消费 | 幂等实现 |
| --- | --- |
| 释放检查 | 天然幂等（`UPDATE ... WHERE status='ACCEPTED'`，影响 0 行即跳过），去重表只用于记录与观测 |
| SLA / 升级告警 | 去重表（eventId + consumer）；通知的天然业务键是 `(ref_type='ORDER', ref_id, target_user_id, event_type)`，可在 `t_notification` 上补唯一索引形成第二道防线 |
| Triage 结果写回 | 去重表 + **状态守卫**：仅当 `triage_status='PENDING'` 时才写回，防止迟到消息覆盖人工修改后的类型 |

> **⚠ 前置缺失（P1 步骤 3 收口核对，2026-09-24）**：上表 SLA/升级告警那行的"第二道防线"依赖
> `t_notification.ref_type` / `ref_id`，但**当前代码根本不回填这两列**——实测业务库 82 条通知，
> `ref_type IS NULL` 与 `ref_id IS NULL` 各为 **82/82**。因此 `UNIQUE(ref_type, ref_id, target_user_id, event_type)`
> **现在建不起来**（NULL 在唯一索引里互不相等，重复通知照样能插入）。**设计本身是对的，缺的是前置写入**——
> 已登记为 P4 的待办（见该阶段"改动范围"），在补上之前不得声称这道防线已生效。
>
> **追加（P5 步骤 2，2026-09-25）**：提交通知这条新链路**没有沿用上面的四元组**，改用
> `UNIQUE(event_id, user_id)`（`t_notification.event_id`，见 D59）。两个理由：① 四元组**缺"事件版本"维度**——
> 同一工单 T0 首次告警与 T0+24h 的第二次催办在该键上完全相同，唯一索引会把第二次**静默漏发**，
> 违反本节自己立下的"幂等键必须是**事件维度**"；② `ref_*` 只有提交通知能回填，SLA/驳回两条老链路仍是 NULL，
> 四元组索引对它们**等于没有约束**。**上面那条前置缺失依然成立**（`ref_*` 回填仍是待办）——
> 但"通知重复"这件事现在由 `event_id` 这道防线覆盖，且**已实测**（重投两次、通知数恒为 1）。

**代价**：多一次 DB 写、多一张需归档的表、事务更长。换来的是"重投永不出错"，这是补偿机制能存在的前提。

### 5.3 延迟消息的实现方案对比

本项目有两类延迟需求，必须分开讨论，否则方案一定选错：

- **固定延迟**：接单后到点检查释放（时限来自 `t_sla_config.accept_minutes`，按工单 type+priority；P1 步骤 5 之前是全局硬编码 30 分钟）。
- **动态延迟**：SLA 到期（`sla_deadline` 由 type/priority 的 `finish_minutes` 决定，因人而异）。

| 方案 | 原理 | 适配固定延迟 | 适配动态延迟 | 代价 |
| --- | --- | --- | --- | --- |
| **TTL + DLX（队列级 TTL）** | 延迟队列设 `x-message-ttl`，到期死信路由到业务队列 | 好（每档位一个队列） | 差（剩余时间不固定，无法预设档位） | 零插件依赖；延迟档位需预先定义；精度受投递顺序影响；消息无法取消 |
| **TTL + DLX（消息级 TTL）** | 每条消息设 TTL | 差 | 差 | **有队头阻塞**（队头消息未到期时，后面已到期的也不投递）；实际仍需配合"档位队列"，退化为队列级方案 |
| **延迟插件 `x-delayed-message`** | 交换机内部保存并在到期时投递，`setDelay(ms)` 支持任意值 | 好 | **好** | 依赖插件（镜像需自备/启用，升级有兼容风险）；延迟消息存在交换机内，**不被队列积压指标覆盖**，堆积难观测；集群/仲裁队列下有行为限制 |
| **时间轮（Redis zset / Redisson `RDelayedQueue` / Netty HashedWheelTimer）** | 应用或 Redis 侧维护到期时间轮，到期再投递 | 好 | 好 | 依赖应用进程存活（重启需重建）；多实例需分布式协调；最终仍需 MQ 兜底投递，多一层组件 |
| **调度扫描（xxl-job 每分钟扫表）** | 按 deadline 查询到期数据 | 够用 | **好** | 精度到分钟；扫表成本随数据量增长（需合适索引）；多实例需调度框架保证唯一 |

**决策**：

1. **固定延迟（释放检查，时限取自配置）→ 首选 `TTL + DLX` 队列级 TTL**（零插件、离线可验证、行为可预测）；**若镜像固定且允许启用插件，可简化为 `x-delayed-message`**。两者都保留 `ReleaseTimeoutScheduler`（迁移后为 xxl-job 任务）作为兜底。
2. **动态延迟（SLA 到期）→ 使用 xxl-job 分钟级扫描，不使用 MQ 延迟消息**。理由：剩余时间不固定，固定档位无法表达；用"短周期轮询队列"会让每条消息被重复投递多次，放大消息量。分钟级精度对"高校报修 SLA 告警"完全够用。
3. **不选时间轮**：它没有解决本项目的问题，只是把调度责任从调度中心搬到应用或 Redis，同时增加一个需要重启重建的状态。

**关于"消息取消"**：接单后到点之前处理人点了"开始处理"，理论上这条延迟消息应该取消。RabbitMQ 的三类延迟方案**都不支持可靠取消**。本项目的正确做法是**到达时校验**（消费时检查 `status='ACCEPTED'` 与 `version`，不匹配就跳过并 ACK）——这也是现有 `releaseOrder` SQL 里 `WHERE status='ACCEPTED'` 的价值。**取消语义靠状态守卫实现，不靠消息删除。**

**P1 步骤 3 落地记录（2026-09-24）：选了 A，并且 A 的前提已被实测验证**

| 验证项 | 结果 |
| --- | --- |
| 官方镜像能否启用插件 | **不能**。官方 `rabbitmq:3-management`（broker 3.13.7）执行 `rabbitmq-plugins enable rabbitmq_delayed_message_exchange` 得到 `{:plugins_not_found, [:rabbitmq_delayed_message_exchange]}`（退出码 70）。插件是官方维护但**不随镜像分发**的社区插件 |
| 因此的处置 | 自建镜像 `deploy/rabbitmq/Dockerfile`：把 45 KB 的 `.ez` **随仓库入库**（境内服务器下载 GitHub release 不可靠），构建期 `--offline` 启用，并加两条构建期断言（broker minor 必须 3.13.x、插件必须出现在已启用列表） |
| 延迟是否真的延迟（delay=60s） | **是**：t+5s 队列 0 → t+30s 队列 0 → **t+62s 队列 1** → t+72s 队列 1 |
| 延迟消息能否扛 broker 重启 | **能**：延迟窗口中途 `docker restart` broker（t+14s 恢复），到点仍入队（t+100s 队列 1） |
| 延迟消息能否扛 **SIGKILL**（`kill -s KILL`，无优雅停机） | **能**（2026-09-24 compose 版复测，容器 `workorder-local-rabbitmq`）：`x-delay=120000`、`delivery_mode=2`，t+5s 队列 0 / `publish_in=1` → t+15s 硬杀（`ExitCode=137`）→ t+34s 节点起、t+85s rabbit 应用就绪（队列仍 0）→ **t+125s 队列 1**（到点入队），取回消息体与 header 一致。**前提是 durable 交换机 + persistent 消息**；"发布后毫秒级被杀"这一段异步落盘窗口**未验证**——边界与备选方案（退化为方案 a：由 outbox 卡 deliver_at）见 `docs/DECISIONS.md` D46 |
| 延迟期间消息在哪 | **在交换机内部**，不在任何队列（`list_queues` 为 0，管理台 API `routed=false`）。观察命令见 `deploy/rabbitmq/README.md` |
| 新发现的代价 | 延迟插件对**每条**延迟消息都返回 `NO_ROUTE` → `mandatory=true` 会给每条消息制造一条假 ERROR。故 `spring.rabbitmq.template.mandatory` 必须为 `false`（见 `INVARIANTS.md` I11、`docs/DECISIONS.md` D37） |

**若将来要放弃插件**：回退方案是 B（TTL+DLX 档位），但必须同时接受"档位与实际 `accept_minutes` 不一致且不报错"的代价，
并补上 `deliver_at` 二次校验。取舍见 D32/D37。

### 5.4 消费顺序与并发消费的取舍

**结论：本项目选择"并发消费 + 状态守卫"，不追求消息顺序。**

判断依据——问一个问题：**同一工单的两条消息乱序到达，会不会产生错误结果？**

- 释放检查：`WHERE status='ACCEPTED'`。若"释放"在"开始处理"之后到达，影响 0 行，跳过。**乱序安全。**
- 状态流转：都带 `version` 乐观锁（`AND version = ?`），旧版本操作必然失败。**乱序安全。**
- SLA 告警：重复由去重表拦截，内容取自 DB 当前状态（瘦消息）。**乱序安全。**
- Triage 写回：由 `triage_status='PENDING'` 守卫 + 去重键。**乱序安全（但需注意不可覆盖人工修改）。**

**代价**：会产生无效消费（一次 DB 写、一次日志）与日志噪音。可接受，因为它换来了横向扩展能力与更简单的队列拓扑。

**并发参数**：保持 `prefetch: 10`（当前值），消费者并发先从 1–2 起。理由：2 vCPU 下放打并发没有收益，反而加剧 CPU 争抢；`prefetch: 10` 配单消费者已能覆盖 ≤10 msg/s 的量级。

**什么时候才需要顺序保证**：当出现"无状态守卫的操作"时（如账户余额扣减、库存扣减）。本项目没有这类操作。**若将来引入，正确做法是按 `orderId` 哈希到固定队列 + 单消费者，或用 Redis 分布式锁按 orderId 串行化——而不是把整个系统改成单队列单消费者。**

### 5.5 死信与重试退避策略

> **实施进展（P4 步骤 2–3，2026-09-25）**：
> ① **退避重投已实现** —— `t_message_retry`（`UNIQUE(event_id, consumer)`）+ `MessageRetryService`（阶梯落账，**在业务事务之外**，
> `REQUIRES_NEW`）+ `MessageRetryDispatchTask`（时间租约抢占后重投，带原 `x-event-id`）；停车上限 5 次 → `PARKED` + ERROR 日志（见 D53）。
> ② **DLX / 停车队列：评估后不采用**（P4 步骤 3）——DB 账本已覆盖它的全部职责，再加一套就是两条并行的失败通道；代价与边界见 **D54**。
> ③ 仍未做："不可重试类失败"的自动分类（当前 `ERROR`/异常一律按可重试处理）。

**先分类，再设计**：

| 失败类型 | 例子 | 处理 |
| --- | --- | --- |
| **可重试**（瞬时） | DB 连接抖动、Redis 抖动、LLM 超时/限流、broker 短暂不可用 | 退避重投，最多 N 次 |
| **不可重试**（永久） | 参数非法、工单不存在、状态非法、消息格式错误 | 直接进死信/停车队列 + 告警，立即 ACK，不浪费重试 |
| **预期内跳过** | 状态已变更导致影响 0 行 | **ACK 且不计入失败**，不重试 |

**退避策略**：`1min → 5min → 15min → 1h → 6h`（5 次），超过后进停车队列并告警，等待人工介入或定时重放。

**实现方式的选择**：

- **RabbitMQ 原生方案（DLX + 停车队列）：评估后不采用**（见 `docs/DECISIONS.md` D54）——
  配置 DLX，消费失败 `basicNack(requeue=false)` → 死信队列 → 由延迟队列（TTL+DLX 的退避档位）或多级死信队列实现退避重投。
  **不采用的理由**：DB 账本（`t_message_retry`）已经覆盖它的全部职责（重试、超限停车、可视化、可人工干预），
  再加一套 DLX 就是**两条并行的失败通道**（同一类事件有两个处理点），并且多一份运维面（死信队列的监控、清理、重放）。
  代价如实记在 D54：**消息不在 broker 的死信队列里，排查看数据库表而不是管理台**。
- **本项目的推荐补充**：失败记录落 `t_message_retry`（含 `next_retry_at`），由 xxl-job 每分钟扫描重投。理由：**退避档位可视化、可手动干预、可统计**，并且与 outbox 共用同一套投递逻辑；代价是多一次 DB 写。

**必须避开的三个坑**：

1. **`default-requeue-rejected` 默认为 `true`**：消费异常的消息会被立刻重回队列，形成"消费—失败—重投"热循环，把队列打成死循环。**必须显式设 `false`**（或确保消费异常路径与 DLX 都显式拒绝）。
2. **手动 ACK 与 `listener.simple.retry.enabled` 叠加**：重试发生在 listener 内部，最终失败后的去向取决于 `MessageRecoverer`。必须显式配置并**用"故意抛异常"的实验验证**，不能照抄 `RABBITMQ-MIGRATION.md` 的样例配置。
3. **不同事件类型不能共用重试策略**：释放检查失败 5 次后应直接放弃（有兜底扫描）；SLA 告警失败 5 次后必须告警人工（不可丢）。把两者放在同一队列、同一重试策略下，一定会出现"某一类消息把队列堵住"。

### 5.6 调度任务的幂等与分片广播

**为什么调度任务也需要幂等**：任务重复触发是常态而非异常——admin 重试、执行器故障转移、人工手动触发、上一轮未完成又到下一轮。

**并发控制（先弄清阻塞策略）**：xxl-job 每个任务都有阻塞处理策略，必须**显式选择**：

| 策略 | 适用 | 本项目用法 |
| --- | --- | --- |
| **单机串行**（`SERIAL_EXECUTION`） | 幂等任务：宁可排队，不丢轮次 | `releaseTimeoutScan`、`slaEscalationScan`（**2026-09-26 裁决**，理由见下）、归档任务（避免同时跑两个归档批次） |
| 覆盖之前调度（`COVER_EARLY`） | 只有最新一轮有意义 | 报表类（可选） |
| 丢弃后续调度（`DISCARD_LATER`） | 扫描类任务，宁可少跑也不重叠 | `outbox-publish`（尚未实现为 xxl 任务） |

> **为什么两个扫描用"单机串行"而不是"丢弃后续"**（2026-09-26 裁决）：① 它与本地 `@Scheduled` 的 `fixedRate`
> **语义一致**——同一个调度线程串行排队、不并发，两路的日志与计数才可比；② 这两个扫描都是**幂等**的
> （释放靠 `WHERE status='ACCEPTED'` 状态守卫、SLA 靠 Redis SETNX），**排队不会算错，丢轮才会晚一步**——
> 而兜底通道的价值恰恰在"这一轮必须跑到"。

**任务级幂等键**：`{taskName}:{businessKey}`。`outbox-publish` 不需要（每条 outbox 记录自带 `event_id` 与状态）；`archive` 需要 `archive:{yyyy-MM-dd}`；`report` 需要 `report:{type}:{date}`。写入带 `UNIQUE` 约束的表或 Redis SETNX，抢占成功才执行。

**分片广播（归档任务专用）**：

- **不要用 `id % shardTotal = shardIndex` 取模**。扩容时 `shardTotal` 变化会让同一行落到不同分片，导致**重复归档或永久遗漏**。
- **正确做法：水位线 + 区间均分**。任务开始时确定本次处理区间 `[startId, endId)`（由水位线表中 `last_archived_id` 与当前 `MAX(id)` 决定），按 `shardTotal` 均分为若干子区间，每个执行器只处理自己的子区间；全部完成后推进水位线。
- **子区间边界必须持久化**（写入任务执行记录表），否则执行器中途崩溃后无法判断"哪些区间已完成"。
- **每批限速**：`LIMIT 500` + 批间 sleep，避免单次大事务与 IO 尖峰。

> **⚠ 反转（2026-09-27，P6 步骤 1 实测）——上面这三条对"归档删除"不适用，未采用**：
> 归档删除改用 **"时间谓词 + `MOD(id, shardTotal) = shardIndex`"**（`@XxlJob("archiveJob")`，见 `deploy/UPGRADE-P6.md`）。
> 关键是**搬运与删除的语义不同**：
> · **搬运**（读一次、写一次）确实需要"区间不重不漏"，扩容导致落点变化就是漏归档 → 水位线有道理；
> · **删除**是"状态驱动 + 幂等可重放"：谓词永远是"锚点列 < cutoff"，每个分片**每轮都重新筛**自己那一份，
>   **这一轮没删到的行，下一轮还会被筛到** → 不存在"永久遗漏"，扩容改落点也不影响正确性；
>   而水位线反而**制造**风险：一旦"水位推进了但删除失败/被中断"，那段区间**永久漏删且无处报错**。
> 代价：单分片要扫过约 `shardTotal` 倍的候选行（`MOD` 用不上索引），换来"不重不漏 + 随时可中断"。
> 依据与实测见 D70。**上面的原文保留**，作为"当时的论证"记录。

**admin 单点的应对（代价换来的诚实结论）**：

- 4G 下**不做** admin 高可用，接受"admin 宕机 → 定时任务暂停"的窗口。
- 缩短窗口的手段：监控 admin 心跳 + 每个关键任务的"最后成功执行时间"，超过阈值告警。
- **本地兜底默认开启，与 xxl-job 并行**（本轮修正 v1）。v1 设计为"默认关闭、admin 挂了再人工开启"——**这个设计是错的**：故障发生时没人在凌晨两点去改配置开开关，需要人工判断的开关等于没有。改为默认开启、长期并行两条路径。理由：`releaseOrder` 靠 `WHERE status='ACCEPTED'` 天然幂等，两条路径重复触发的唯一代价是**日志噪音**，而收益是"admin 挂了释放检查照常工作"。这是"用无害的冗余换可用性"。

### 5.7 RabbitMQ 内存水位触顶的下游影响（v1 缺失；这是 outbox 的第二重价值）

**关键事实：`vm_memory_high_watermark` 触顶后是"阻塞生产者"，不是"拒绝消息"。**

后果链：

```
broker 内存到达水位
  → RabbitMQ 停止读取发布请求（TCP 层不再读取），向生产连接发送 connection.blocked
  → 生产端 convertAndSend 调用挂起（默认无超时）
  → 占用调用方线程
  → 调用方线程池被耗尽 → 该线程池承载的所有功能不可用
```

也就是**"内存问题被转化成可用性问题"**——而且故障点看起来在应用侧（线程耗尽），真正的原因在 broker。

**为什么本方案的架构天然缓解了它**：因为投递不再由用户请求线程发起，而是由**独立的 outbox 投递任务**（§5.1）从 outbox 表发起。broker blocked 时，被阻塞的是投递任务线程，**不是提交工单的用户请求线程**；用户请求在写库（含 outbox 记录）后就返回了。**这是 outbox 的第二重价值**（第一重是防丢），v1 只写了第一重。

**仍然必须做的三件事**（不能因为 outbox 缓解就不做）：

1. **生产端必须设连接超时**（`spring.rabbitmq.connection-timeout`）——否则挂起的不是几秒钟，而是直到 TCP 超时。
2. **broker blocked 期间，outbox 记录保持 `PENDING`**，由退避重投；**不得丢失、不得标记为 `SENT`**。判定标准：只有 publisher-confirm 返回 ack 才允许改写状态。
3. **投递任务自身的并发度必须受限**（单线程或小线程池）——否则每个投递线程都被 blocked 挂住，最后连退避重投也跑不起来。

**监控项增加**：连接 blocked 状态（`connection.blocked` / 管理台 blocked 计数）、队列深度、**outbox 中 `PENDING` 的记录数**（这个数值持续上涨就是 blocked 或消费者故障的第一信号）。

---

## 六、CPU 与磁盘 IO 的应对（v1 最大的空白）

v1 在 §1.3 正确指出"真正会先出问题的是 CPU 与磁盘 IO，不是内存"，但 P1–P7 里**没有任何针对措施**——识别了矛盾却没有解法。本节补齐。

### 6.1 为什么 CPU 与 IO 才是真瓶颈（量化）

- **2 vCPU 上跑 7 个容器**：MySQL、RabbitMQ、xxl-job-admin、后端四方争抢。RabbitMQ 在大量延迟消息同时到期时是 CPU 密集型（单核可吃满）；MySQL 的 redo/binlog 刷盘、归档任务的范围扫描、后端的 GC（2 vCPU 下 GC 线程会与业务线程抢核）都会争同一批核。
- **IO 更低**：低配云盘（20–40 IOPS 档）上，MySQL redo 刷盘 + RabbitMQ 持久化/mqueue 落盘 + Redis AOF 重写三方叠加，就会让在线查询出现百毫秒级抖动。
- **结论**：**内存不足会 OOM（有明确信号），CPU/IO 不足只会"变慢"（信号模糊、容易被误判为代码问题）**。所以必须靠"错峰 + 限速 + 限核 + 监控"来管理，而不是靠加内存。

### 6.2 任务错峰表（a）：哪些任务不能同时跑

| 任务 | 建议时间窗 | 资源特征 | 不能与谁同时跑 |
| --- | --- | --- | --- |
| `archive`（归档） | 凌晨 02:00–04:00 | IO 密集（批量读 + 写 + 删），CPU 中 | **严禁**与 `report-generate`、压测同时跑 |
| `report-generate`（报表聚合落表） | 凌晨 04:30 之后（归档结束后） | IO + CPU 中高（跨月区间聚合） | 归档、压测 |
| `releaseTimeoutScan`（释放扫描） | 每 60s 常驻 | 极轻（走 `idx_sla`） | 无 |
| `slaEscalationScan`（SLA 扫描） | 每 60s–5min 常驻（建议 1 分钟） | 轻（走 `idx_sla`） | 无 |
| `outbox-publish`（投递） | 每 5–10s 常驻 | 轻（少量行 + 网络） | 无，但投递线程数必须受限（§5.7） |
| `retry-replay`（退避重放） | 每 60s 常驻 | 轻 | 无 |
| 压测 / 演示准备 | **必须提前与上述批量任务错开** | 极高（CPU + IO 双高） | 归档、报表、备份 |

**硬规则**：同一时刻只允许**一个**"重任务"（归档 / 报表 / 压测 / 备份）在跑。这条可以落成 xxl-job 的调度时间表 + 一句团队约定，不需要额外组件。

### 6.3 容器 CPU 限制（b）：防止单容器吃满 2 vCPU

| 容器 | 建议 `cpus` 上限 | 理由 |
| --- | --- | --- |
| MySQL | `1.0` | 查询与刷盘都需要核；给多了会在归档时吃满，给少了会让在线查询排队 |
| 后端应用 | `0.8` | 业务请求 + MQ 消费 + outbox 投递；与 admin 错开峰值 |
| xxl-job-admin | `0.3` | 主要是心跳与调度触发，几乎不耗 CPU，**限死以隔离它对业务的干扰** |
| RabbitMQ | `0.5` | 消息量 ≤10 msg/s，仅延迟齐发时吃 CPU |
| Redis | `0.3` | 单线程模型，给多也无用 |
| Nginx | `0.2` | 静态资源与反代 |
| **合计** | ≈ **3.1**（> 2） | 这是**上限之和**，不是预留；正因为总和超过 2，才需要 `cpus` 限制来保证"任何单容器都不能独占全部核" |

**注意**：`cpus` 限制过严会导致 CFS 调度节流（throttling），表现为偶发延迟尖刺。建议先按上表设，再观察 `container_cpu_cfs_throttled_seconds_total` 决定是否放宽。

### 6.4 归档的限速与 IO 隔离（c）

- **批大小**：`LIMIT 500` 一批（v1 已写），批间 `sleep 200–500ms`，避免连续打满 IO。
- **避开在线峰值**：归档时间窗固定在 02:00–04:00（校园报修的低峰），且**必须可被打断**（任务内检查中断标志）。
- **不阻塞在线查询**：读写都走主键/索引范围（`id` 区间 + `idx_created`），不做全表 `UPDATE`/`DELETE`；单批事务尽快提交，避免长事务持有行锁与 undo。
- **限速可验证**：归档期间在线查询接口 P95 不劣化（P6 的验收标准已包含这一条）。

### 6.5 监控指标（d）

| 类别 | 指标 | 告警阈值建议 |
| --- | --- | --- |
| CPU | 整机 CPU 使用率、**steal**（云主机被宿主机抢占的时间占比） | steal > 5% 说明"这台机器不是你的全部"，需上调配额而非优化代码 |
| CPU | 各容器 `container_cpu_cfs_throttled_seconds_total` | 持续增长说明 `cpus` 限制过严（§6.3） |
| IO | `iowait`、磁盘队列深度、`Innodb_data_fsyncs` | iowait 持续 > 20% 说明 IO 已成瓶颈 |
| DB | **MySQL 慢查询数**、`Innodb_buffer_pool_reads`、连接数 | 慢查询增长率突增是最早的退化信号 |
| MQ | 队列深度、**连接 blocked 状态**、死信数 | 见 §5.7 |
| 投递 | **outbox `PENDING` 记录数**、`t_message_retry` 待重放数 | 持续上涨即为故障第一信号 |
| 内存 | 各容器 RSS 与 `mem_limit` 的比值 | > 85% 预警（余量只有 0.8–1.4G，见 §1.3） |

---

## 七、实施顺序

排序原则：**先能演示、后做优化**；每一阶段都可独立验证、可独立回滚；不出现"必须一次上线全部改造"的阶段。

**本版的排序目标（新增）**：让**第一个面向业务的亮点尽早到达**——即"提交接口 P99 从约 5 秒降到 100 毫秒量级"（F1-4 Triage 异步化）。v1 把 Triage 异步化排在 P5，位于 xxl-job、outbox、消费幂等三项基础设施之后，结果是要讲这个亮点得先讲完三章基础设施。

**执行顺序（v2 重排，阶段编号保持不变以免打断既有引用）**：

```
P0 → P1（含 outbox）→ P4 → P5 → P2 → P6 → P7
```

| 序 | 阶段 | 为什么排在这个位置 | 预估工作量（人日） |
| --- | --- | --- | --- |
| 1 | **P0**（P0a + P0b） | 正确性与配置基线，是所有改造的前提 | **2.5**（P0a 2.0 + P0b 0.5） |
| 2 | **P1 + outbox**（原 P1 与 P3 合并） | 合并理由见下 | **4.0** |
| 3 | **P4**（消费幂等 / 死信 / 退避） | 与 P1 属同一知识块（生产端 → 消费端），紧接其后无需重新进入上下文 | **3.0** |
| 4 | **P5**（Triage 异步化 + 提交通知） | 只依赖 MQ 与消费幂等，**不依赖 xxl-job**；它是第一个业务亮点，必须尽早 | **2.0** |
| 5 | **P2**（XXL-Job 迁移） | 排在 P5 之后：它是纯基础设施迁移，没有新的业务价值；但 P6 依赖它 | **2.0** |
| 6 | **P6**（归档 + 报表聚合落表） | 依赖 P2 的调度与分片 | **3.0** |
| 7 | **P7**（演练与固化） | 收口 | **1.5** |
| | **合计** | | **18.0 人日** |

**人日口径（本轮补充）**：上表是 **"1 人全职日"** 口径（1 人日 = 1 人专注工作 1 个自然工作日，约 6–8 小时有效产出）。**非全职投入必须换算成日历时间**，否则会严重误判进度：按每周实际投入 12–20 小时计，约合 **1.5–2.5 人日/周**，因此 **18 人日 ≈ 7–12 周日历时间**；这也意味着 (b) 中"第 2 周末到第 3 周初看到第一个业务亮点"的结论**只在全职投入下成立**，若每周投入 12–20 小时，则应改为**第 5–8 周**。

**P1 与 outbox 合并的理由（v1 的 P1 注定被 P3 重写）**：v1 让 P1 先实现 `RabbitMQPublishServiceImpl`（`convertAndSend` 直发），再让 P3 改成"写 outbox"。这意味着 P1 交付的那个类**注定要被 P3 删掉/重写**——两轮工作量，且中间态（直发）恰好是双写漏洞的状态。合并后一次做对：P1 直接实现"业务事务内写 outbox + 独立投递任务"，同时天然获得 §5.7 的第二重收益（broker 阻塞不占用户请求线程）。

### 7.0 前置澄清（不写代码，但要先定论）

三件事不定，后面每一步都会返工：

1. **`ISSUES.md` 权威版本**：`PROJECT_MAP.md` 记录仓库 HEAD 版（约 81KB）与仓库外 `adr\ISSUES.md`（约 47KB）内容不一致，任务进度的权威来源必须先确认。
2. **`CLAUDE.md` 技术栈红线与本方案是否对齐**：该文件已回到工作区根目录，其中把 RabbitMQ 3.x、XXL-Job 2.4.0 列为"不可违反的技术栈红线"，但同时要求"严禁过度设计"。本方案对两者的使用范围（固定延迟用 MQ、批作业用调度）需要与该红线确认一致，避免后续实现时出现"红线要求全量接入"与"业务论证只支持部分场景"的冲突。
3. **通知权威路径**：明确"SLA 告警由调度器直发"还是"由消费者发"，二者只能留一个（见 3.4 第 1 条）。

### P0 · 基线与正确性修复（拆为 P0a / P0b）

P0 是两轮新增项的合并结果，按"是否涉及数据迁移与前端改动"拆成两批。**顺序要求写死：P0a 第 9 项（删除测试残留）必须先于 P0b 第 13 项（存量类型迁移），反过来会做大量无用功**——那 108 条残留不属于业务数据，对它们做类型迁移纯属浪费。

#### P0a · 配置基线与正确性修复（无数据迁移、无前端改动）

| # | 事项 | 说明与依据 |
| --- | --- | --- |
| 1 | Redis 淘汰策略 `allkeys-lru` → `volatile-lru` | 见 §1.5 第 1 条：否则会话/幂等键/去重键会被无差别淘汰 |
| 2 | 各容器补 `mem_limit` 与 CPU 限制 | 见 §1.4 与 §6.3；上限之和（约 4.3G 内存 / 3.1 核）刻意大于物理值，靠限制保证单容器不独占 |
| 3 | **实测当前 4 容器 RSS**（替代 v1 的"直接上调堆与 buffer pool"） | 见 §1.4：不在没有负载时先调参；用 `docker stats --no-stream` + 压测组合态测（§1.3 组合态验证） |
| 4 | 实测验证容器内 swap 可用性（`memswap_limit` 语义） | 见 §1.3 第 1 条：`swappiness=10` 是否生效取决于 cgroup 版本，不得假定 |
| 5 | **I4 修复**：`sla_deadline` null 守卫 + `type` 枚举校验 + 兜底配置 + 兜底触发计数 | 见 `INVARIANTS.md` §2（已定稿 a-2）与 `BUSINESS-SCOPE.md` F1-1 情形一/情形二 |
| 6 | `ensureSlaConfigComplete()` 启动自检（复用 `ensureAdminSurvival` 模式） | 见 `INVARIANTS.md` I5：把"漏配"从运行期静默降级变成启动期显式报错 |
| 7 | **测试污染根因修复（类级）** | 见 `INVARIANTS.md` §三：独立测试库 / 类级 `@Sql` 清理 / `@AfterEach` 按前缀删除，三选一，**必须在类级** |
| 8 | 重写 `testSubmitOrder_slaDeadlineNull` 为真正断言 | 见 `INVARIANTS.md` §2「I4 的证据基础」：现方法用 `OTHER/0`（配置存在）且无 `slaDeadline` 断言，是空测试；修复后应断言得到兜底值 |
| 9 | **删除 108 条 `TST-` 测试残留** | 见 `INVARIANTS.md` I4(c)：删除，不是回填、不是标记。**必须先于 P0b 第 13 项** |
| 10 | 探针 P1–P13 纳入例行检查 | 见 `INVARIANTS.md` §四；P13（测试不污染业务库）是第 7 项的验收 |

| 项 | 内容 |
| --- | --- |
| 验证方式 | ① `docker stats --no-stream` 记录 4 容器实测 RSS，与 §1.2 重算后的估算对照，余量不得低于 0.6G；② 空载 + 组合态（压测 + 高连接数）各跑 30 分钟，观察无 OOM、无重启；③ `redis-cli INFO memory` 确认 `maxmemory_policy=volatile-lru`；④ 登录若干账号后制造内存压力，确认会话未被淘汰；⑤ 容器内 `free` / `cat /proc/meminfo` 确认 swap 可见且能被换出（第 4 项）；⑥ 导入 `sql/init.sql` 到临时库后，`admin` + README 明文能登录（探针 P12）；⑦ 连续两轮 `mvn test` 后业务库行数不变（探针 P13）；⑧ P4/P5/P11 探针结果达标 |
| 风险点 | 抬高内存上限后若机器实际可用内存低于标称 4G（虚标或被其他进程占用）会直接 OOM，必须先用 `free -m` 与 `docker stats` 核实；`cpus` 限制过严会引入 CFS 节流尖刺（§6.3）；第 9 项的删除**不可逆**，执行前必须先跑一次 P4 留档（当前 108 条，全部为 `TST-` 前缀） |

#### P0b · 类型枚举替换与数据迁移（必须在 P0a 第 9 项之后）

| # | 事项 | 说明与依据 |
| --- | --- | --- |
| 11 | 8 行 SLA 配置数据替换 + 2 处列注释 | `sql/init.sql:15` 与 `:131` 的注释**已在本轮改完**；8 行数据仍是旧类型集合（文件内已加 `-- R4 待办` 标注）。**行数保持 8 行不变**，且 `OTHER + 普通` 必须存在 |
| 12 | `sql/data-generator.sql:73` 的类型集合同步 | 否则造数脚本会产出配置表覆盖不到的类型（本轮新发现） |
| 13 | 存量工单旧类型值迁移 | 清理残留后仅剩 3 行 `WO-` 工单（实测），迁移成本极低；**若先做第 13 项再做第 9 项，就要为 108 条垃圾数据处理类型，纯属无用功** |
| 14 | 前端 `OrderCreateView.vue` 的类型选项同步 | 否则界面仍只能选旧类型，与后端枚举校验冲突（P0a 第 5 项会直接拒绝提交） |
| 15 | 密码 seed 统一 + README 同步 | **已在本轮完成**：删除覆盖用 `UPDATE`，只留一条 `INSERT`，哈希与 `admin123` 一一对应并经 BCrypt 校验；README 已同步 |

| 项 | 内容 |
| --- | --- |
| 验证方式 | ① P11 探针（8 条配置，事前型）= 8；② P5 探针（工单中出现但配置表没有的组合）为空；③ 前端创建工单页只能选到四类新类型，且提交成功；④ 探针 P12（清库导入后 `admin` 能登录）通过；⑤ `sql/init.sql` 导入临时库 `exit=0` 后 `DROP`（本轮已验证该姿势可用） |
| 风险点 | 类型替换与 8 条配置必须**同一次上线**：先上校验后上配置会让新类型工单全部被拒；存量迁移前必须确认没有别的表冗余了类型值（当前仅 `t_sla_config` 与 `t_work_order`）；第 11 项改完前，`sql/init.sql` 处于"注释是新集合、数据是旧集合"的中间态，**这是有意为之并已在文件内标注**，不要误判为遗漏 |

### P1 · 引入 RabbitMQ **+ outbox**（执行顺序 2/7；原 P1 与原 P3 合并）

| 项 | 内容 |
| --- | --- |
| 改动范围 | ① 新增 `RabbitMQConfig`（交换机/队列/绑定 + `publisher-confirm` + `publisher-returns` + `default-requeue-rejected: false`）；② 新增 `t_event_outbox` 表（`event_id` 唯一、`status`、`retry_count`、`next_retry_at`）；③ 发布接口改为 `publish(OrderEvent)`（事件维度 `eventId`）；④ 业务侧由 `afterCommit → convertAndSend` 改为**事务内写 outbox**；⑤ 新增 `outbox-publish` 投递任务（独立线程/任务，`FOR UPDATE SKIP LOCKED` 或状态抢占，**并发度受限**）；⑥ `MockMessagePublishServiceImpl` 加 `@Profile("!prod")`，真实实现走 outbox；⑦ 新增 `OrderReleaseListener`（手动 ACK + 三态判定）；⑧ **保留 `ReleaseTimeoutScheduler` 作为兜底**；⑨ 收敛 30 分钟常量（引用 `BUSINESS-SCOPE.md` F4-1 的三件事结论） |
| 验证方式 | ① 抢单后管理台可见延迟消息且 30 分钟内不被消费；② 30 分钟后工单转 `RELEASED`；③ `docker stop rabbitmq` 后抢单，1 分钟内由兜底扫描释放——证明"MQ 挂了不丢"；④ 手动重复投递同一条消息，确认只释放一次（状态守卫）；⑤ 消费异常时确认消息进 DLX 而非无限重回队列；⑥ **故障注入**：在 commit 之后、投递之前 `kill -9` 应用，重启后 outbox 中该条从 `PENDING` 变 `SENT` 且消息被投出；⑦ **计数守恒**：发消息数 = outbox `SENT` 数 = 消费记录数；⑧ broker 停止期间 outbox 记录保持 `PENDING` 且不被误标 `SENT`（§5.7） |
| 风险点 | 延迟插件不可用（退回 TTL+DLX）；消费者线程里 `OrderLogAspect` 的登录上下文（`operatorId=0`）语义；`prefetch` 过大导致单消费者堆积；**outbox 表增长**（清理任务在 P6）；**投递任务并发度未受限时，broker blocked 会把投递线程全部挂住**（§5.7 第 3 条） |
| **兜底路径说明（v1 的承诺要写清）** | 本阶段承诺的"停 MQ 仍能释放"依赖的是**进程内 `@Scheduled`**。这一点必须在交付说明里写明：**该保证在 P2 迁移后会退化**（见 P2 的倒退标注） |

### P4 · 消费端幂等、死信与退避（执行顺序 3/7）

> **P1 进展（2026-09-24）**：**步骤 1–5 全部完成并提交** —— ① 事件模型 + `MessagePublisher`；② `t_event_outbox` + 事务内写路径；
> ③ 投递任务 + 延迟交换机 + 开关；④ `releaseOrder` 三态 + `OrderReleaseListener` + ACK/NACK 契约；
> ⑤ **`accept_minutes` 常量收敛**（兜底扫描与 MQ 路径同源，修 G5/I8，提交 `e7a159b`）。
> **步骤 6（服务器验证）**：升级路径与操作清单已交付（`deploy/UPGRADE-P1.md`、`sql/hotfix-p1-outbox-init.sql`、D51）；
> 三个验证（停 broker 仍能释放 / 后端强杀不丢 / 重复投递只释放一次）与 5 容器真机 RSS **待服务器输出回填**。
>
> 已可证明（含真机实测）：投递侧闭环（outbox → 交换机 → 队列，到点进队）→ **消费侧闭环**（到点被消费 → 工单 `RELEASED` → 队列深度回 0）；
> 停 broker **不误标 SENT**（retry_count++ 且退避）；broker 恢复后**补投成功**；**broker 被 SIGKILL 后延迟消息仍在**（到点照样投递）；
> **后端进程被强杀后 outbox 记录被补投**；"重复投递"与"工单已被 START"两种情形都判 `SKIPPED` + ACK（只释放一次）。
>
> **仍未做（不要按"已完成"引用）**：死信队列与阶梯退避（P4 步骤 2/3）、SLA 扫描去重改为事件级、通知表回填 `ref_type/ref_id`；
> 因此消费端 **ERROR 仍 ACK**（无 DLX 时 NACK 等于静默丢消息，见 D43），且**失败消息不会被自动重试**。
> **已补**：消费去重表 `t_consume_record`（P4 步骤 1，2026-09-25，见 D52）——重复投递命中去重键直接 ACK，不再依赖状态守卫单独承担幂等。
> 另：P1 起第 5 个容器（RabbitMQ），内存形态变化见 §1.6.7。

| 项 | 内容 |
| --- | --- |
| 改动范围 | 新建 `t_consume_record`（`UNIQUE(event_id, consumer)`）；`OrderEvent` 全链路携带 `eventId`；取消 `sla_notified:{orderId}` 粗粒度键，改为事件级去重（含 H1 的 `eventVersion` 递增，见 §5.2）；配 DLX + 停车队列；新建 `t_message_retry` 与 `retry-replay` 任务（退避 1m/5m/15m/1h/6h）；**新增待办（P1 步骤 3 收口登记）：让通知写入回填 `t_notification.ref_type/ref_id`**——当前 82/82 全为 NULL，不补则 §5.2 的第二道防线（唯一索引）无法生效。另外：本阶段起扫描 SQL 应直接排除已通知工单，替掉 `SlaEscalationScheduler` 现在"查出后靠 Redis 键跳过"的做法（该方法注释已标明"降日志级别只是消噪音，不代表修好"） |
| 验证方式 | ① 人为重复投递同一 `eventId` 两次 → 只产生一条通知、一条消费记录；② 让消费者抛异常 → 消息进死信 → 按退避重放 → 最终成功；③ 超过 5 次 → 进停车队列并触发告警；④ 构造"工单 24h 内两次合法告警"（SLA 超时 + 驳回超限，`v1`/`v2`）→ 两条通知都发出（对比现状会被吞掉一条） |
| 风险点 | 去重表与业务写的事务边界写错 → 消息被静默吞掉（本阶段最大风险，必须专门测）；去重表成为写入热点 |
| 为什么紧跟 P1 | 生产端（outbox）与消费端（幂等/死信）是同一知识块的两半：**P1 解决"发得出去"，P4 解决"重复也不出错"**。中间插入别的阶段会让实现者重新进入上下文，且 P1 交付的消费者在三阶段内都处于"没有幂等保护"的状态 |

### P5 · 业务改造：Triage 异步化 + 提交通知（执行顺序 4/7；第一个业务亮点）

> **实施进展（P5 步骤 1，2026-09-25）**：**Triage 异步化已完成** —— `submitOrder` 不再同步调 LLM（缺字段走兜底 + `triage_status='PENDING'` + 同事务发 `ORDER_TRIAGE` 事件）；
> 新增 `workorder.order.triage.queue` 与 `OrderTriageListener`/`OrderTriageConsumeService`；写回只覆盖"提交时为空"的字段（元信息 `missingFields` 随消息走），
> 按 H4 以 `created_at` 为基准重算 `sla_deadline`、过期则立即告警并计为 H1 首次告警；失败复用 P4 重试账本。
> **实测对照（本机 stub LLM 3s 延迟，并发 30）**：改造前 P99 **3110.9ms**、连接数恒 21（池占满）→ 改造后 P99 **230.7ms**、吞吐 31×；并发 5 时 P99 **33.5ms**（见 D55/D56 与 README §九）。
> **仍未做**：提交通知（步骤 2）、前端"分类中"状态与放开必填（步骤 3）。**注意**：上面的数字是 stub 口径，真实模型基线需在配 key 的机器上重取。
>
> **实施进展（P5 步骤 2，2026-09-25）**：**提交通知已完成** —— 提交事务里只多写一行 `ORDER_SUBMITTED` 事件；
> 接收人解析（按角色 `HANDLER` 群发）与站内信写入都在消费端（`OrderSubmittedConsumeService` / `OrderSubmittedListener`）。
> 两道幂等防线：去重表 `t_consume_record` + **通知表新键 `UNIQUE(event_id, user_id)`**（选择理由见 D59；
> §5.2 原提的四元组 `(ref_type, ref_id, user_id, event_type)` 缺 version 维度、且 `ref_*` 只有新链路能填，故未采用）。
> 端到端实测（本机真 broker + 自建延迟镜像）：提交 → outbox `SENT` → 队列深度回到 0 → 处理人站内信 1 条（API 可见）；
> 同一事件重投两次（第二次先删掉去重记录）→ 通知数恒为 1；工单已被抢走时重投 → `SKIPPED` 且不通知。
> **提交 P99 的同机对照（2026-09-25 重测，取代本节初版那对数字）**：三行同参数（`OMIT_TYPE=1`、`TRIAGE_MODE=async`、
> 并发 30、预热分离、每请求计时、各行跑前清空 outbox、各跑 2 遍且第二遍反向顺序）——
> **① 改造前 3130.5ms/6185.6ms（P50/P99）、② 改造后无通知 96.2ms/193.8ms、③ 改造后+通知 149.4ms/264.4ms**
> （第 2 遍：① 3136.7/6199.8、② 101.6/206.4、③ 130.9/232.1）。完整表与 type/优先级分布见 `README.md` §9.1。
> ③ 相对 ② 的代价是**每次提交多一行 outbox insert**（每单 DB 写 3→4 条，本机吞吐 209–220 → 142–164 req/s），
> **与接收人数无关**，而不是把 N 次插入搬回提交事务。
> ⚠ 初版那对数字（179.2–202.0 / 187.3–210.5、P50 107.5–110.3）来自**计时口径错误的 ps1**
> （`WaitAll` 后统一停表 = 每波最慢值），且"改造前"那行用的是**读不到 chunked 请求体的桩**（每次调用都是 400）。
> 两个工具缺陷的定位、修复与影响见 D60 与 §1.6.5。
> **实施进展（P5 步骤 3，2026-09-25）：本步已完成**——前端放开 `type`/`priority` 必填 + 三态（分类中/已分类/分类失败）显示，
> 后端补齐数据来源与状态可达性。**该步实际修了三个缺陷**（前两个是批准项，第三个是同类因果链上发现的）：
> ① `triage_status` 未暴露 → `WorkOrderVO` 加字段并在 **两处**手写 `toVO`（service + controller）赋值；
> ② 没有任何语句写 `FAILED` → 账本转 `PARKED` 时**同事务**把工单置 `FAILED`（`markTriageFailed`，带 `PENDING` 守卫）；
> ③ **"LLM 失败被洗成一次成功"**——`triage()` 曾把所有失败 catch 成 `TriageResult.fallback()`（`OTHER/普通`），
> 于是 LLM 一次没调通也会写成 `triage_status='DONE'`，而"失败→账本→阶梯→停车→FAILED"这条链**一次都走不到**
> （旧测试用 `@MockBean` 让失败 `thenThrow`，**mock 掩盖了实现分歧**）。现改为抛 `TriageUnavailableException`。
> **实测（真栈 + 真 broker，`README.md` §9.2）**：成功路径 提交 **t+125ms** 返回（body 带 `triageStatus=PENDING`）→
> **t+6.9s** 写回 `NETWORK/1`、SLA **8h→1h**；失败路径（桩 401）→ 账本 1 行、工单仍 PENDING →
> 阶梯走完 → 账本 **6/PARKED** 且工单 **FAILED**。新增探针 **P17**（PENDING 超 1 小时 = 0）在该场景下 **PASS**。

> **P5 收口补充（2026-09-25）**：① 前端两处显示修正——`operator_id=0` 渲染为「系统」（原来显示"用户0"）、
> `ACTION_MAP` 补 `TRIAGE` →「AI 分诊修正」（原来时间线里直接显示原始码），见 D63；
> ② **AI 分诊评测集落地**：`scripts/triage-eval-cases.json`（20 条，含 6 条信息不足）+ `scripts/triage-eval.py`
> （走真链路跑分，输出类型/优先级准确率、保守率与失败清单；**真实准确率待配了 key 的机器上重跑**（**已跑：见 D68，2026-09-26 两遍 14/14**），
> 本机只有桩，跑出来的是机械自检），见 README §5.4 与 D63；
> ③ **"人工指定 vs AI 判定"的界面区分：评估后暂不做**（列表页看不出来源，详情页已有 `TRIAGE` 日志），
> 方案与代价见 D64；④ 曾有结论称"信息不足时 prompt 行为随机"——**已更正：无证据，不作为修复**，
> 若将来加 prompt 保守规则只能写成"加固"且需评测集给出失败样例（D63）。
>
> **按评测结果改 prompt（2026-09-25 同日）**：真机跑分给出基线 **类型 85.7%（12/14）、信息不足组保守率 4/6**，
> 逐条判读为 O1=真实模型错（期望值不改）、D2=边界模糊（改 prompt）、I4/I6=可识别倾向（保守规则依据）。
> 据此改 prompt 两处：**① 四类类型边界写具体**（照明/门窗/家具/锁具→DORM；供水供电中断或危险→UTILITY；
> 网络/WiFi/网口/业务系统→NETWORK；建议咨询等非故障类→OTHER）；**② 信息不足时保守**（OTHER+普通，并在 reason 写明依据不足）。
> 同时按"prompt 与评测集一起演进"收窄了原本的真歧义用例（O2→DORM、E2→NETWORK，各留 `baseline_expect_types`），
> 并让 `reason` 真正落进写回日志（否则"写明依据"就是写了不生效）。**改后的数字待真机重跑**——（**已跑：见 D68，2026-09-26 两遍 14/14**）
> 本机无 LLM key，只做了机械自检（桩）。详见 D65 与 `README.md` §5.4。
>
> **门槛调整（2026-09-26）**：`llm.api.timeout` **5000 → 15000ms**（真机各条端到端 2.0–9.1s，
> 5s 落在分布中部 → 长输入偶发失败；连接与读取两个超时都由这一个值驱动）；
> 评测脚本等待上限 **90 → 150s**（≥ 首次重试退避 1 分钟），并把"判错 / 超时未完成 / 分诊失败"**分开计数**、
> **超时不再从分母里消失**（对外口径：超时与失败都计为失败、分母保持 14）。
> 代价（含本轮新发现）：最坏 ≈30s 才降级、启动自检最坏多等 10s、消费端在事务里占连接的时长从 5s 变 15s
> （当前消费并发=1、池 20，可接受；**调大并发前必须重算**）。详见 D66。
>
> > 已被 D67 取代：LLM 调用自 2026-09-26 起在事务外。"占连接 5s→15s"与"调大并发前要重算连接"均不再适用，新约束见 D67。
>
> **结构性修复（2026-09-26，D67）**：把**分诊的 LLM 调用移出事务**——三段式「① 事务外调 LLM → ② 事务内去重+写回+SLA+日志
> → ③ 事务外写重试账本」，P4 的两条规则（去重与业务写同事务、账本在业务事务外）一个字没动。
> 实测（10s 桩、并发 3、6 单）：修复前 `innodb_trx` 中位数 **3**（92/94 样本 ≥2，即连接被事务持续持有）→
> 修复后 **0**（0/93）；并发从 1 提到 5 时 20 单总耗时 101.3s → 21.0s，且**不再以连接占用为代价**。
> 代价：多出"重复调一次 LLM"的窗口（调用完成→事务提交之间崩溃 / 并发重复投递），写在类注释、D67 与 `scripts/tx-probe.ps1`。
> **方法学反转**：`Threads_connected` 峰值**判定不了**这件事（池连接不缩），要看 `information_schema.innodb_trx`。

| 项 | 内容 |
| --- | --- |
| 改动范围 | `submitOrder` 移除同步 LLM 调用，改为兜底值落库 + `triage_status=PENDING`；新增 triage 事件与消费者；结果写回 + **SLA 重算 + 修正日志**（H4 定稿：`created_at` 基准 + 过期立即告警，且该告警计为 H1 的首次告警）；新增"提交后通知处理人/部门"事件；`NotifyChannel` 预留外部渠道；前端放开 `type` 必填并要求展示"分类中"状态 |
| 验证方式 | ① 提交接口 P99 从 ~5s 降到 <100ms（前后对比压测，**本阶段的演示重点**）；② LLM 宕机/未配 key 时提交仍成功，`triage_status` 保持 PENDING 并告警；③ 异步结果写回后核对 `type/priority/sla_deadline` 三者自洽；④ 重复投递 triage 结果不覆盖人工修改；⑤ 提交后处理人收到站内信，重复投递只收一条；⑥ 界面能演出"分类中 → 网络故障/紧急 → SLA 截止时间收缩"完整时序（`BUSINESS-SCOPE.md` §6.1 第 1 步） |
| 风险点 | **SLA 重算遗漏导致"类型与期限不符"的静默错误**；triage 结果与用户手工填写的字段冲突（必须以人工填写优先）；LLM 成本与限流；前端放开必填后，用户不选类型将成为常态，**triage 链路从"可选优化"变成主链路** |
| 为什么提到 P2 之前 | 它**只依赖 MQ 与消费幂等（P1 + P4），不依赖 xxl-job**。v1 把它排在 P4 之后、P2 之后，导致第一个能讲给业务听的成果（提交从 5 秒到 100 毫秒）被压到第 5 个阶段 |

### P2 · 引入 XXL-Job，迁移两个 `@Scheduled`（执行顺序 5/7）——**已完成（2026-09-27）**

> **P2 完成（2026-09-27）——三步各自的判据，以及"为什么它比控制台上的绿灯强"**
>
> | 步骤 | 判据（可执行） | 为什么它比界面绿灯强 |
> | --- | --- | --- |
> | ① admin 部署 | `docker compose up -d --no-deps xxl-job-admin` 之后：`docker compose config` 出 **6 个服务**；`docker ps` 里 `workorder-mysql / redis / rabbitmq` 的 `CreatedAt` **仍是 `2026-09-24 23:22:38`**（证明没被连带重建）；`free -m` 原文留档（6 容器 `MemAvailable 1750`） | "控制台能打开"只说明 Web 起来了：**说明不了堆上限真生效**（本镜像 entrypoint 把 `JAVA_OPTS` 放到 `-jar` 之后，必须看 `/proc/[0-9]*/cmdline` 或 `Picked up JAVA_TOOL_OPTIONS`），也**说明不了没牵连业务容器**（只能看 `CreatedAt` / `free -m`） |
> | ② 执行器接入 | `xxl_job_registry` 出现 `EXECUTOR / work-order-system / http://…:9999/` 行，**隔 35s 再查 `update_time` 必须前进** | "执行器在线"的绿灯在 2.4.0 里**默认根本不打**（`registry success` 是 DEBUG、失败才是 INFO）；而且**一行没人刷的僵尸 registry 也会有行**——只有 `update_time` 前进才证明心跳在刷 |
> | ③ 两个任务迁移 | 执行器启动日志**两行** `xxl-job register jobhandler success, name:releaseTimeoutScan / slaEscalationScan`；点一次执行后 `xxl_job_log` 的 `trigger_code/handle_code=200` 且 **`handle_msg` 带业务摘要**；停 admin 时后端日志仍有 `[release-scan] 触发来源=local …` 且按 60s 出现 | 控制台的"执行成功"只反映**一次 HTTP 往返被受理**；handler 到底有没有注册、业务到底跑没跑，要看注册日志与 `handle_msg`。而"停 admin 兜底仍在跑"是**P2 那处可靠性净倒退是否被堵住的唯一判据**（只留调度中心 = "admin 挂就没人释放"） |
>
> **留痕状态（别把它读成全都在仓库里）**：① 的原始输出在仓库（§1.6.8 与 `deploy/UPGRADE-P2.md` §6）；② 本机原文见 `deploy/UPGRADE-P2.md` §8 附注，**真机输出待贴**；
> ③ 由委托方在服务器真机验过（2026-09-27），但 **`xxl_job_log` 行原文**与**停 admin 期间 `触发来源=local` 的时间戳序列**尚未入档 → **待贴**（复核命令见 `deploy/UPGRADE-P2.md` §9 末）。

> **P2 步骤 1 进展（2026-09-26）：调度中心已部署，执行器未接** ——
> `deploy/docker-compose.yml` 新增第 6 个服务 `xxl-job-admin`（`xuxueli/xxl-job-admin:2.4.0`），
> 同实例建 `xxl_job` 库（建表脚本 `sql/xxl-job/tables_xxl_job.sql` 已随仓库，2.4.0 官方版）；
> `mem_limit == memswap_limit = 512m` + **显式 `JAVA_TOOL_OPTIONS=-Xmx256m -XX:MaxMetaspaceSize=128m`**（JVM 默认按宿主内存取上限，4G 上约 1G，
> 会撞穿 512m 上限被 OOMKilled——本机实测设 -Xmx256m 后 RSS ≈ 358.8 MiB）；
> **参数口径（部署实际值）**：`JAVA_TOOL_OPTIONS: -Xmx256m -XX:MaxMetaspaceSize=128m`——**不要用 `JAVA_OPTS`**：本镜像 entrypoint 把它放在 `-jar` 之后（= 应用参数，不生效），见 §1.6.8 教训 2。
> 注意与 §1.6 里 admin 的 **`-Xmx192m（预演，RSS 299.2 MiB）`** **不可混比**——堆上限不同、差 ~60 MiB，
> 详见 §1.6 的追加注（引 §1.6.1 开头的"三列不可混用"声明）；容量规划仍按 §1.6.2 的上界口径。
> 管理台只绑 `127.0.0.1:8080`（与 MySQL/Redis/RabbitMQ 同一条收紧原则）。
> **backend 刻意不 `depends_on` admin**：admin 停摆不影响业务，这是本节"可靠性净倒退"的两条缓解措施之一。
> **本步判据（已在本机逐条验过）**：① `docker compose config` 出 6 个服务且 6 组 mem_limit/memswap_limit 两两相等；
> ② `xxl_job` 库 8 张表 + 默认账号 `admin` 存在；③ 管理台可打开（`/xxl-job-admin/` → 302 跳登录、`/toLogin` → 200、
> 标题 `<title>任务调度中心</title>`）、启动日志 `Started XxlJobAdminApplication`；④ 停 admin 后 backend 登录仍 200、
> 进程内兜底扫描照常。**未做的（属 P2 后续）**：后端执行器配置与 `@XxlJob` 迁移、双发路径收敛、`@Scheduled` 下线。
> 服务器操作清单见 `deploy/UPGRADE-P2.md`；**6 容器整栈真机内存基线待回填**（届时 §1.6 的 4/5/6 三种形态并列）。

> **P2 步骤 2a 进展（2026-09-26）：执行器已接入并**在本机验完注册**，任务未迁** ——
> 新增 `com.workorder.config.XxlJobConfig`（`@ConditionalOnProperty(name="xxl.job.executor.enabled", havingValue="true")`，
> **默认关**：否则本地/CI 会去连 admin、还占 9999 端口，`@SpringBootTest` 会连带受影响）；
> `application.yml` 的参数按本项目惯例写成 `${VAR:-正确默认值}`（`XXL_JOB_ADMIN_ADDRESSES` 本地默认 localhost、容器内由 compose 注入服务名；
> `XXL_JOB_EXECUTOR_ENABLED` 默认 false；`XXL_JOB_ACCESS_TOKEN` 与 admin **同源同值**）；
> backend 容器新增三个键 + `xxl-job-logs` 卷挂到 `/data/applogs/xxl-job`（不挂就写在容器层，重建即丢）。
> **本机判据（全部命令，未用浏览器）**：`mvn -o test` **208 passed**（开关默认关 → 测试不受影响）；
> 起本地 admin + 后端开关打开后：`xxl_job_registry` 出现 `EXECUTOR / work-order-system / http://192.168.2.13:9999/` 行，
> **35 秒内 `update_time` 前进**（心跳 30s 节奏）；组不存在→跑幂等 `INSERT` 后 `xxl_job_group` 有 `work-order-system`；
> 容器内 `/dev/tcp` 测宿主机 `9999` **可达**（所以本机把注册链路完整验过，Windows 那条限制在本机没发生）。
> **两个坑（都实测过，已写进 UPGRADE-P2 §8）**：① token 两侧不同值 → 执行器日志 `code=500 msg=The access token is wrong`
> （**别往网络方向查**）；② 2.4.0 的 `registry success` 是 **DEBUG** —— **判据只看 DB 的 registry 行与 update_time**。

| 项 | 内容 |
| --- | --- |
| 改动范围 | 部署 xxl-job-admin（同实例新建 `xxl_job` 库）；后端加执行器配置；`ReleaseTimeoutScheduler` → `releaseTimeoutScan`（每分钟＝60s，与本地兜底同频；单机串行）；`SlaEscalationScheduler` → `slaEscalationScan`（**每 5 分钟＝300s，与本地兜底同频**；**改频须同时改两处**＝本地 `@Scheduled(fixedRate)` 与调度中心调度配置；单机串行）；**解决双发路径，只留一条**；调度器里的同步通知改为投递消息；**`@Scheduled` 本地兜底默认保留并与 xxl-job 并行**（§5.6） |
| 验证方式 | ① admin 控制台看到执行器在线；② 手动触发一次任务，确认执行日志与 DB 状态一致；③ 停掉后端容器，admin 的任务调度记录出现失败/阻塞告警；④ 连续两轮触发时确认**单机串行生效**——两轮**排队执行、不并发**（不是"丢弃后续"）；⑤ 单条超时工单在 1 分钟内被扫描到（`releaseTimeoutScan` 的节拍）；⑥ **停掉 admin 后本地兜底仍在跑**（验证并行设计）；⑦ 两路同窗口时"业务效果只发生一次"（判据与实测见 D69） |
| 风险点 | admin 单点（任务停摆窗口）；阻塞策略配置不当导致重叠执行；任务执行线程池与 MQ 消费线程争抢 2 vCPU；两套调度并行会产生重复触发（靠 `releaseOrder` 的状态守卫吸收，代价是日志噪音） |
| **⚠ 可靠性净倒退（必须写明）** | **P1 承诺的"停 MQ 后仍能释放"，其兜底是进程内 `@Scheduled`。P2 把 `@Scheduled` 迁到 xxl-job 后，该保证退化为"MQ 挂、且 admin 健在，才不丢"。这是 P2 引入的净倒退。** 缓解手段有两条，缺一不可：① 本地兜底**默认开启**并与 xxl-job 并行（§5.6 已改，取消 v1 的"需人工开启"开关）；② P7 增加"同时停 broker 与 admin"的组合故障演练 |

> **§P2 的 handler 名唯一真源 = `@XxlJob` 注解值**（`ReleaseTimeoutScheduler:80` / `SlaEscalationScheduler:93`）：
> 文档与它不一致时**以注解为准**。（2026-09-26 修正：本节表格原写**连字符形式**的 handler 名，与实际实现的驼峰名不符。）

> **P2 步骤 2b 进展（2026-09-26）：两个 handler 已落地；真机 admin 里的任务仍未建** ——
> 两个 Scheduler 各抽 `scanOnce(String source)`，本地 `@Scheduled`（`local`）与调度中心 `@XxlJob`（`xxl`）**并行**，
> 日志带来源标记（`触发来源=local/xxl`）——那是"双跑判据"的载体，不是装饰。
> 控制台建任务的字段表 + 四项危险区配置（单机串行 / 超时 120s 与 300s / 重试 0 / 过期 DO_NOTHING）见 `deploy/UPGRADE-P2.md` §9。
> **双跑幂等的机制证据（`innodb_trx` 的两条 `LOCK WAIT`）与业务事实（`status/version`、日志行数、通知行数）见 D69**——
> 其中 B 段如实记录了"自然时序下没撞上守卫"，只有 C 段（人为叠窗）才是决定性证据。
>
> **（2026-09-27 更新：上面这句"真机 admin 里的任务仍未建"已过期**——两个任务已在真机 admin 建好并按 §9 判据验过；
> 现状见本节开头的"P2 完成"块，配置字段见 `deploy/UPGRADE-P2.md` §9。）

### P6 · 归档与报表（执行顺序 6/7）——**步骤 1 已完成（按保留期删除）；搬运与报表未做**

> **P6 步骤 1 进展（2026-09-27）：三张表的"按保留期分批删除"已落地，归档搬运与报表未做** ——
> 新增 `@XxlJob("archiveJob")`（`com.workorder.scheduler.ArchiveJob` + `ArchiveTarget` 白名单 + `ArchiveParams`）：
> **白名单**只有 `consume_record` / `message_retry` / `outbox_sent`（即 `t_event_outbox` 的 **SENT** 行）；
> 参数 `tables=...;retentionDays=...;batchSize=...;maxBatches=...`，分片取 `XxlJobHelper.getShardIndex()/getShardTotal()`；
> 每批一条 `DELETE ... WHERE <时间谓词> AND MOD(id, shardTotal) = shardIndex LIMIT n`（**一批一事务**，批后 `XxlJobHelper.log`）；
> **预算用完是正常返回**（"本轮删除 N 行，未完下一轮继续"，`handleSuccess`），判据在 `t_archive_log.outcome`
> （`DONE` / `BUDGET_EXHAUSTED` / `FAILED`）。
> DDL 与索引（`t_archive_log` / `t_job_watermark` / `idx_created_at` / `idx_status_sent_at`）**唯一出处**
> `sql/hotfix-p6-archive.sql`，`init.sql` 只留一行指针；控制台配置与服务器验证清单见 `deploy/UPGRADE-P6.md`。
>
> **本机实测（专用库 `wo_p6a`，一次性，已清理）**：三张表 3600 行一轮删净且 30 天内的行全部保留（含 PENDING 的 outbox 行一行未动）；
> `maxBatches=1` 时只删 1000 行、日志打"预算用完，未完下一轮继续"、`outcome=BUDGET_EXHAUSTED`；
> 紧接着重跑 `deleted_rows=0`；`shardTotal=3` 三次合计 3000 == 单分片全量 3000 且剩余 0。
> **EXPLAIN 判据有一条重要口径**：小表（几百行）上优化器会选全表扫（`key=NULL`），
> 且 outbox 在"几乎全过期"的形状下会选旧的 `idx_dispatch` 的 status 前缀——
> **必须在"稳态形状 + 有代表性行数"的表上判索引**，详见 D70。
> **仍未做**：`t_work_order` / `t_work_order_log` 的**搬运**（含下表那条"归档前必须处理孤儿日志"）。

> **P6 步骤 2 进展（2026-09-27）：日报汇总已落地（不分片）** ——
> 新增 `@XxlJob("dailyReportJob")`（`DailyReportJob` + `DailyReportParams` + `DailyReportWriter`）：
> **只算到昨天**（今天的数据还在写，算出来会漂）、**一天一个事务**、**水位与结果行同事务**（先写结果后推水位，
> 反过来崩在中间就是那天永久漏算）；`from`/`to` 补数模式照算照写但**不动水位**；
> 另外有一条**自愈**：水位那天在 `t_daily_report` 里没有行时从那天重算（缺行可自愈；"值被改错"走补数模式）。
> **指标口径**（哪些按 `created_at`、哪些按完结时刻、分母是谁、逾期是时点快照）写在
> `sql/hotfix-p6-report.sql` 的列注释与 `mapper/DailyReportMapper.xml` 里，**两处成对维护**。
> DDL 见 `sql/hotfix-p6-report.sql`（`t_daily_report` + 步骤 3 预留的 `t_daily_report_part`；水位表复用步骤 1 的 `t_job_watermark`，
> job_key = `daily-report`），控制台配置见 `deploy/UPGRADE-P6.md` §5。
>
> **本机实测（专用库 `wo_p6b`，一次性，已清理；原始输出见 D71）**：3 天数据集逐列与手写 SQL 一致；
> 23:59:59 与 00:00:00 分别落在两天；`report_date = CURDATE()` 的行数为 **0**；
> 正常模式连跑两次 + 补数模式重算一次，三次输出逐列一致；水位推进到昨天后不再动；
> 删掉最后一天的行 → 下一轮**自愈**；用触发器让水位写失败 → **结果行跟着回滚**（`row_exists=0`），
> 移除触发器后重算恢复——这是"同事务"的直接实证。
> **仍未做（步骤 3）**：分片（`shardTotal` 参数本步只接受不使用，`t_daily_report_part` 只建表不写）。

| 项 | 内容 |
| --- | --- |
| **⚠ 归档前必须处理孤儿日志** | 业务库现有 **约 52.9 万条 `t_work_order_log` 对应仅 3 条工单**（`data-generator` 造数与历史测试残留的产物：日志表的 `order_id` 指向已不存在的工单，其 `order_no` 也大量是 `WO-20250101-*`/`WO-20260601-*` 这类造数单号）。**按工单状态归档时这些孤儿日志不会命中任何规则**（它们没有可关联的工单，取不到 status），会被永久留在主表里、让"归档后主表变小"这个目标落空。归档任务必须显式处理：先按 `order_id NOT IN (SELECT id FROM t_work_order)` 识别孤儿，单列一条清理规则（保留 N 天或直接归档到日志归档表），并在任务里统计孤儿条数、与预期核对 |
| 改动范围 | 新建归档表（`t_work_order_archive` / `t_work_order_log_archive`）或归档库；新增 `archive` 任务（分片广播 + 水位线 + 批间限速）；新增 `report-generate` 任务与结果表——**S6 重定义：只做"定时聚合落表"，不做 Excel/PDF 文件导出**（跨月区间的聚合是实时接口做不到的，故保留；文件生成、下载鉴权、临时文件存储与清理的成本没有业务收益，见 `BUSINESS-SCOPE.md` §4 N10）；新增 outbox 已发送记录的清理任务 |
| 验证方式 | ① 手工触发归档 → 抽样核对源表与归档表行数一致；② 中途杀掉任务 → 重跑不重复、不遗漏；③ 归档期间在线查询接口 P95 无劣化（证明限速有效）；④ 分片数从 1 改为 2 → 区间划分正确、无重复处理；⑤ `report-generate` 写入的聚合结果表 SHALL 与实时看板的同口径读数一致（跨月区间取结果表、当区间取实时查询，两者差额需可解释） |
| 风险点 | 大事务/长锁影响在线服务；**分片取模扩容导致重复或遗漏**（必须用区间而非取模）；归档后历史查询接口需要跨表（提前确认是否保留在线查询能力） |

### P7 · 演练与固化（执行顺序 7/7）

| 项 | 内容 |
| --- | --- |
| 改动范围 | 无功能改动，只做演练、监控补齐与文档 |
| 验证方式 | ① **单点故障演练**：停 broker、停 Redis、停 admin、MQ 积压到水位、重复投递、容器 OOM 重启、时区错位（容器 TZ 与 MySQL `connectionTimeZone` 一致性）各跑一次并记录恢复表现；② **组合故障演练（本轮新增）**：**同时停掉 broker 与 admin**，验证释放检查仍工作——这是验证"本地 `@Scheduled` 兜底默认开启"是否真的生效的唯一方式，也是 P2 那处可靠性净倒退是否被堵住的判据；③ 压测：模拟 10× 峰值（约 50 单/分钟）确认无消息积压；④ **组合态资源观测**：压测 + 归档 + 高连接数同时发生时记录 RSS 与 CPU（§1.3 组合态验证）；⑤ 监控覆盖：队列深度、死信数、outbox PENDING 数、任务最后成功时间、连接 blocked 状态、容器内存接近度、MySQL 慢查询数（§6.5）；⑥ 更新 `RABBITMQ-MIGRATION.md` 使其与最终实现一致（该文档描述的路径已与实际漂移） |
| 风险点 | 演练暴露的容量问题（尤其是 2 vCPU），可能需要回退到"削减任务频率"或"升级到 8G"；时区问题会直接导致 SLA 提前/延后 8 小时触发；组合故障演练可能暴露"admin 停摆 + MQ 停摆"下兜底并未生效（若 P2 把 `@Scheduled` 真正下线了） |

> **P7 收尾项（累积登记；2026-09-27 更新）——演示前必须清完**
>
> 1. **演示库数据清理（最高优先）**：`t_work_order = 542`，其中**逾期工单 ≥200 条**。
>    - **≥200 是怎么来的**：SLA 扫描 `findSlaExpired` 带 `LIMIT 200`，真机上**每轮都被拉满**、且全部走
>      `SLA通知已发送过，跳过重复通知`（Redis `sla_notified:*` 在起作用）→ 说明符合条件的行 ≥200。
>      **精确条数本轮未查**：`SELECT COUNT(*) FROM t_work_order WHERE status IN ('PENDING','ACCEPTED','IN_PROGRESS') AND sla_deadline < NOW();`（**待补**）。
>    - **代价**：每轮 200 行的查询成本（I8 记录的"查出后跳过"老行为，根治在 P4 的扫描 SQL 收紧）+ 日志噪音。
>    - **判据**：清理后重跑 **P4 / P5 / P13** 确认归零，并把 542 → 清理后的数字**贴回**。
> 2. **同批处理的其它残留**（D68 已登记，本轮确认仍未清）：**400 张压测单**、id 区间 **849–868** 与 **889–908**（早期评测/验证留下的）。
>    - **例外，保留不删**：**888 / 902** 两条账本＝"失败 → 账本 → 阶梯重投自愈"的实测凭证（D68）。
> 3. **删除方法照 D19**：先按**最宽口径**统计（`order_id` 与冗余列 `order_no` 两个口径都跑、取最大值）留档，再删。
> 4. **admin 默认口令**：仍是 `admin/123456`（只绑回环、公网不可达，故未阻塞演示）——登记在 `deploy/README.md` 的"上线前收尾项"，**上线前必须改**。

### 阶段依赖关系

```
P0a（配置基线 + 正确性修复）
  └─► P0b（类型枚举替换 + 数据迁移）      ← 必须在 P0a 第 9 项之后
        └─► P1（RabbitMQ + outbox）
              └─► P4（消费幂等 / 死信 / 退避）
                    └─► P5（Triage 异步化 + 提交通知）   ← 第一个业务亮点
                          └─► P2（XXL-Job 迁移）
                                └─► P6（归档 / 报表聚合落表）
                                      └─► P7（演练与固化）
```

**依赖说明（v2 改为严格串行）**：v1 允许 P1 与 P2 并行（互不依赖），v2 把 P2 排在 P5 之后，原因是 P2 属于"纯基础设施迁移、无新业务价值"，把它插在业务亮点之前只会推迟亮点到达；而 P6 依赖 P2 的分片与调度，这个依赖链仍然成立。**P5 必须等 P4**（否则异步 triage 会重复调用 LLM、重复写 SLA）；**P0b 必须等 P0a 第 9 项**（否则要为测试垃圾数据做迁移）。整链串行带来的代价是总工期更长、并行度下降，换来的是每一阶段都在上一阶段验证过的地基上施工。

### 附：每阶段"可演示"的验收动作

| 序 | 阶段 | 演示动作（一镜到底） |
| --- | --- | --- |
| 1 | P0a | `docker stats` 展示 4 容器实测 RSS + 余量；`redis-cli INFO memory` 展示 `volatile-lru`；临时库导入 `init.sql` 后用 `admin`/`admin123` 登录成功；两轮 `mvn test` 后业务库行数不变 |
| 1 | P0b | 前端创建工单页只能选到 `NETWORK/UTILITY/DORM/OTHER` 且提交成功；P11 探针 = 8、P5 探针为空 |
| 2 | P1 | 抢单 → 管理台看到延迟消息 → 停掉 MQ 仍能超时释放；**`kill -9` 后端重启后消息仍被投出**（outbox 生效） |
| 3 | P4 | 同一消息投两次 → 只发一条通知；故意失败 → 进死信 → 退避后成功 |
| 4 | P5 | 提交工单 <100ms 返回 → 界面"分类中" → 变"网络故障/紧急" → SLA 截止时间收缩（第一个业务亮点） |
| 5 | P2 | admin 控制台手动触发 SLA 扫描 → 秒级返回并留下执行记录；**停掉 admin 后本地兜底仍在跑** |
| 6 | P6 | 触发归档 → 中断 → 重跑 → 数据一致、在线查询无感；聚合结果表与实时看板读数一致 |
| 7 | P7 | **同时停 broker 与 admin** → 释放检查仍工作；停 Redis 演练不产生数据错误 |

---

## 附：本方案明确不做的事

1. **不修改任何代码**（本轮边界）。
2. **不引入业务论证不支持的中间件**：Kafka、RocketMQ、PowerJob、MongoDB、Elasticsearch、Seata、配置中心、服务网格一律不引入，理由见 2.8 与 4.1/4.3。
3. **不做 xxl-job-admin 高可用、不做 MySQL 主从**：4G 机器没有余量，属于"要换机器才能要的能力"。
4. **不引入分布式事务框架**：所有一致性都在单库事务内，跨进程一致性用"outbox + 幂等消费"解决，成本低一个数量级。
5. **不把抢单链路的并发方案改成分布式锁/MQ**：单条原子 SQL + 乐观锁已是该场景的最优解，改造只会引入新风险。
